# План работ — текущая итерация

## Принцип: ОДНА правка → коммит → ждать билд → следующая

---

## 🔴 В работе

### Шаг 1. RPC `get_daily_pnl_series` v3 ✅ применена в БД
Возвращает: revenue + commission + logistics + storage + acquiring + deduction + penalty + cogs + tax + net_profit + margin_pct.

### Шаг 2. Фронт: расширить DailyPoint + fetchDailyRevenue 🔄 в процессе
Добавить поля storage/acquiring/cogs/tax/marginPct в типы и query.

### Шаг 3. Единый график «Динамика P&L» на /pnl
- Линии: Доходы (зелёный), Комиссия (фиолетовый), Логистика (sky), Хранение (amber), Эквайринг (cyan), Cogs (orange), Налог (slate), Маркетинг (pink)
- Линия Маржа% — на отдельной правой Y-оси (двойная ось)
- Чекбоксы в легенде: вкл/выкл каждую линию
- В легенде по умолчанию: ✅ Доходы, ✅ Маржа; остальные off (показывать по запросу)
- Ширина — full

### Шаг 4. Тот же график на /dashboard вместо двух отдельных
- Заменить `RevenueExpensesChart` + `ProfitMarginChart` единым `PnLChart`
- На дашборде по умолчанию: ✅ Доходы, ✅ Расходы (общие), ✅ Маржа

### Шаг 5. Проверить «цифры неправильные»
Что именно — спросить пользователя:
- 229 677 ₽ в Доля каналов?
- Топ-5 суммы?
- KPI значения?

---

## 🟡 Открытые вопросы / не решённые

- Фото: `tm/1.webp` — проверить после деплоя, работает ли у всех 80 SKU
- График маржи поуже по высоте — обсуждалось, отложено

---

## ✅ Сделано в текущей пачке (PR #116)

- RPC `get_pnl_by_period` v3 (R14 + cogs + tax + все расходы)
- RPC `get_daily_pnl_series` v3 (та же логика по дням)
- /dashboard новая раскладка (3 столбика + бриф+каналы вверху + пульс внизу)
- /pnl новый расширенный режим (таблица per-SKU, экспорт CF_PL.xlsx)
- /products каталог: убраны Канал/Бренд/Послед.продажа, tooltip на Метки/Хватит/TVV
- Бейдж «vs позавчера» с цветом
- Убран нерабочий поиск из топбара
- Tooltip про критичные SKU
- KPI «Маржа» отдельной карточкой
- Топ-5 на дашборде
- Подписи каждой недели на графике
- Авто-гранулярность графика (день/неделя/месяц)
- Фикс PAD_X (Y-axis не обрезается)
- Категории = subject_name
- Фото fallback через WB-баскеты (формула + tm/1.webp)
- Прессы для чеснока — is_active=false
- Правила: «UI без горизонтального скролла» (rules.md §7 + LLM_Wiki)
- Правила: «не дёргать про токен при 401» (rules.md §6 + LLM_Wiki)
