# Excel-файлы владелицы — справочные

Эталонные/исходные данные, переданные владелицей 2026-06-19 для:
- автосебеса (`china_order_items`, `fulfillment_costs`, `supplies_transport`)
- эталонной сверки P&L (telegram-alerts и pnl-расчёты)

> **04.09.2026 в репозитории оставлена одна книга — `UNIT_economics_cogs_tariffs.xlsx`.**
> Решение владелицы: держать только ту, которую читают скрипты. Шесть остальных
> удалены из рабочего дерева; данные из них система уже забрала в базу.
>
> Достать удалённый файл, если понадобится:
> `git log --diff-filter=D --oneline -- tasks/excel-from-owner/<имя>.xlsx`,
> затем `git checkout <коммит>^ -- tasks/excel-from-owner/<имя>.xlsx`.
>
> Вес репозитория при клонировании это не уменьшает: файлы остаются в истории git.
> Чтобы убрать их и оттуда, нужен переписанный заново журнал изменений всего
> репозитория — отдельная операция, к ней возвращаться по решению владелицы.

## Файлы

| Файл | Размер | Содержимое | В репо |
|---|---|---|---|
| `CF_PL_2026_dashboard.xlsx` | 265 KB | **Эталон P&L/CF.** Листы: Дашборд, PL общ, PL WB (нед), PL WB, PL OZON (нед), PL OZON, CF (нед), CF, проверка, Оригинал. Дата 2026-06-16. | удалён 04.09 |
| `UNIT_economics_cogs_tariffs.xlsx` | 709 KB | **Юнит-экономика.** Листы: Юнит, Юнит Карго, Расчет Карго, Себес, Себес 1С текущ, Структура артикула, Тарифы ОЗОН, Тарифы ВБ, Нормативное время доставки. | **оставлен**, читает `scripts/export_unit_weights.py` |
| `UNIT_WB_weekly_2026.xlsx` | 1631 KB | **Недельные срезы продаж/остатков WB** за 10 недель. Колонки: Арт ВБ, Арт поставщика, Название, ШК, Остаток на начало нед, Себестоимость на 1 ед, Себест. остатков, Оборачиваемость (дн). | удалён 04.09 |
| `FF_supply_plan_by_warehouse.xlsx` | 1917 KB | **ТЗ для фулфилмента** — поставки по складам ВБ (Электросталь, Коледино, Тула, Рязань, Адыгейск, Астана, Волгоград, Домодедово, Екатеринбург, Казань...). | удалён 04.09 |
| `Stocks_summary_wb_ozon.xlsx` | 232 KB | **Сводный по остаткам**: Артикул ВБ, Артикул ОЗОН, Арт. поставщика, Название, ШК + распределение по складам. | удалён 04.09 |
| `Order_china_128122.xlsx` | — | **Заказы 1688/Китай.** Колонки: ссылка, фото, комментарий, кол-во шт, цена юань, сумма юань, доставка, Арт. Мой, Арт ВБ. Источник закупочной себестоимости. | удалён 04.09 |
| `CF_PL_WB_refresh.xlsx` | — | **Акции WB 1-11** + лист «Рассчет акции» + шаблоны ВБ. Эталон промо-расчёта владелицы для сверки с нашим `/promo`. | удалён 04.09 |

## Удалено

| Файл | Когда | Почему |
|---|---|---|
| `CF_PL_2026_april_copy.xlsx` | 03.09.2026 | копия дашборда на 20.04 «для diff»; сверка давно неактуальна, история в git |
| `CF_PL_2026_dashboard.xlsx` | 04.09.2026 | эталон P&L; сверка сделана, расчёты в `get_pnl_by_period` |
| `CF_PL_WB_refresh.xlsx` | 04.09.2026 | эталон промо-расчёта; данные акций теперь тянет `fetch-wb-promotions` |
| `FF_supply_plan_by_warehouse.xlsx` | 04.09.2026 | 1,9 МБ ТЗ фулфилменту; разовый документ, в базу не грузится |
| `Order_china_128122.xlsx` | 04.09.2026 | заказ 1688; импортируется через UI `/products/costs`, хранить в репо не нужно |
| `Stocks_summary_wb_ozon.xlsx` | 04.09.2026 | сводка остатков; живые остатки идут из `fetch-wb-stocks` ежедневно |
| `UNIT_WB_weekly_2026.xlsx` | 04.09.2026 | недельные срезы; скрипт `import-unit-wb-weekly.py` читает файл из выгрузки, а не отсюда | удалён 04.09 |

## Регулярность

Владелица обновляет эти файлы регулярно («освежает»), и у неё уже есть версия
юнит-экономики новее той, что лежит здесь. Правильный путь — импорт через UI
(загрузка XLSX), чтобы не зависеть от ручного коммита в репозиторий: тогда книге
вообще не нужно тут лежать. Пока такого импорта для юнит-экономики нет, файл
остаётся — его читает `scripts/export_unit_weights.py`, собирая веса и габариты
в `docs/seo/unit-weights.json` для книги карточек.

## План использования

### 1. Автосебес из заказов 1688
**Источник:** `Order_china_128122.xlsx`
**Таблица:** `china_order_items` (sku_id, qty, price_cny, delivery_cny, date)
**Маршрут:** парсер `apps/web/src/shared/lib/parsers/china-order.ts` → API `apps/web/src/app/api/import/china-order/route.ts` → insert в `china_orders` + `china_order_items`.

**Как импортировать:** на странице `/products/costs` — кнопка «Импортировать заказ Китай (XLSX)» (создаёт заказ и позиции, маппит Арт ВБ → `sku_catalog`).

### 1b. Себестоимость «до ВБ» из UNIT-экономики
**Источник:** `UNIT_economics_cogs_tariffs.xlsx`, лист «Себес» (колонка F — финальная себестоимость).
**Маршрут:** парсер `apps/web/src/shared/lib/parsers/unit-cogs.ts` → API `apps/web/src/app/api/import/unit-cogs/route.ts` → обновляет `sku_catalog.cost_price_rub` + вставляет запись в `sku_cost_history` (маппинг по «Код WB», фолбэк по «Арт. Поставщика»).
**Как импортировать:** на странице `/products/costs` — кнопка «Импортировать себестоимость UNIT (XLSX)».

### 2. Тарифы фулфилмента и Cargo
**Источник:** `UNIT_economics_cogs_tariffs.xlsx` (листы «Тарифы ВБ», «Тарифы ОЗОН», «Расчет Карго»)
**Таблицы:** `fulfillment_costs`, `supplies_transport` (предварительные имена)

### 3. ТЗ для фулфилмента по складам
**Источник:** `FF_supply_plan_by_warehouse.xlsx`
**Назначение:** распределение количеств по складам ВБ — нужно сверить с нашим `/supplies` и автоматизировать формирование такого ТЗ.

### 4. Сверка P&L
**Источник:** `CF_PL_2026_dashboard.xlsx` (лист «PL WB», «PL WB (нед)»)
**Назначение:** прогнать недельный P&L владелицы через нашу `get_pnl_by_period` и сверить — это покажет точное методологическое расхождение (для калибровки telegram-алертов).

## ВАЖНО

- Эти файлы могут содержать чувствительную бизнес-информацию (себестоимость, маржа, конкретные SKU). Не публиковать.
- Использовать как read-only ссылку для разработки. Когда автосебес будет готов через UI/CSV-импорт — файлы можно архивировать (но не удалять).
