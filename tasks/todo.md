# Tasks — SellerBase

> Живой TODO. Принцип: один список, один порядок приоритета. Устаревшие секции удаляем, не накапливаем.

---
## [актуально] 2026-09-02 — Первая заливка состоялась, восемь групп разблокированы

### Три новые группы разобраны по трём источникам (02.09, вечер)

Владелица прислала три среза Эвирмы (`мячи для собак`, `скакалки спортивные`,
`фитнес резинки`, по 100 позиций каждый) и подтвердила, что Джем и ключи
Промопульта тоже на руках. Условие §1.2.2 выполнено — три группы разобраны
и добавлены в книгу отдельными листами.

| группа | карточек | лист | источники |
|---|---|---|---|
| Игрушки для животных | 6 | «Игрушки для животных» | Джем + Эвирма + Промопульт + **фото слайдов** |
| Скакалки | 4 | «Скакалки» | Джем + Эвирма + Промопульт, **фото нет** |
| Фитнес-резинки | 4 | «Фитнес-резинки» | Джем + Эвирма + Промопульт, **фото нет** |

**Разбор игрушек от 21.08 отменён целиком.** Он строил конструкцию не по фото
и ошибся: у `101` форма регби, а не круг, диаметр 7 см, а не 6 (взята «Глубина
предмета 6»), поверхность в шипах у `101` и протектор у `102`, подвес —
эластичный ремешок 28 и 29 см, а не «верёвка».

**Главная находка группы игрушек: «на верёвке» — неправда.** Подвес резиновый,
покупатели ставят за это единицы («мяч на РЕЗИНКЕ!», «прилетает по руке»).
При этом запросы про резинку у нас в топ-10 с конверсией 44–56 %, а «на верёвке» —
позиции 23–32 с конверсией 27–40 %. Правда здесь и честнее, и прибыльнее.
Второе: «суперпрочный» убрано — мяч полый, и отзывы бьют ровно в это слово.

**Цены против рынка (Эвирма 02.09).** Скакалки 1 799 ₽ при медиане 345 ₽
и максимуме топ-100 в 1 426 ₽ — дороже нас в срезе нет никого. `ACRB3FR101CL`
2 659 ₽ при максимуме топ-100 в 2 239 ₽. Мячи 735–899 ₽ при медиане 508 ₽.
Отсутствие в топ-100 объясняется остатками (владелица: ждёт партию, дальше
реклама и подъём), ценовой разрыв — нет. Вопрос к юнит-экономике до рекламы.

**Запрос `мячи для собак` — не наш.** В топ-20 среза 20 карточек из 20 —
реклама, органики нет вовсе; в топ-18 мягкие игрушки, наборы, пуллер-кольцо
и кость, резинового мяча на ремешке нет ни одного.

Новое в листе «Размеры и вес»: у всех четырёх мячей **упаковка 11×8×6 меньше
предмета 28×8×6** — 28 см с ремешком не влезают в коробку 11 см. Либо неверна
высота предмета, либо габариты упаковки.

### Фото скакалок и фитнес-резинок пришли — два пробела закрыты, три открыты

Владелица прислала слайды 02.09 вечером: три карточки скакалок из четырёх
(`101BC`, `101BL`, `101RD`) и три резинки из четырёх (`3FR101CL`, `4FR300CL`,
`5FR200CL`). Разборы пересобраны по ним.

**Скакалки — материал был на слайдах всё это время.** Поле «Материал изделия»
пустое у всех четырёх карточек, а на инфографике написано: трос — 49 сплетённых
нитей из нержавеющей стали в резиновой оболочке; ручка пятислойная
(силикон · алюминиевый сплав · антикоррозийный слой · прозрачная основа ·
матовый слой); подшипник из нержавеющей стали; регулировка нажимным замком;
ручка 17 см, до подшипника 13,5 см. Ничего из этого не попало ни в поля,
ни в описания — а конкуренты в Эвирме выносят материал прямо в заголовок.

Заодно закрылись два вопроса: цвет `101BL` («голубой · бирюзовый») подтверждён
слайдами, правка не нужна; отзыв «подшипники перетирают трос» объяснён —
перетирается резиновая оболочка в месте входа в ручку, а не сталь, и это
вынесено в блок «Честно» всех трёх описаний.

**Фитнес-резинки — три расхождения слайдов с карточками:**

| карточка | в кабинете | на слайде |
|---|---|---|
| `5FR200CL` | нагрузка макс. **30**, «10–30 кг» | X-HEAVY **45 кг** (9/18/27/36/45) |
| `4FR300CL` | ширина **6 см**, в описании «60 мм» | **50 мм** |
| `4FR300CL` | нагрузка макс. **25** | **23 кг** (на ленте 50 lbs) |
| `3FR101CL` | окружности нет нигде | **75 см**, в сложенном виде 38 см |

Первое — крупное: товар занижен в полтора раза, при том что Джем показывает
по этой же карточке заказ с запроса `резинки для фитнеса тканевые 60кг`.

Слайды также подтвердили: мешочек есть у `3FR101CL` и `5FR200CL`, у `4FR300CL`
его нет — пустое поле «Упаковка» отражает реальность. Но значения разошлись:
«чехол» у одного, «сумка» у другого при одинаковом мешочке на фото.

**Чего по-прежнему нет:** фото `ACRB1SK201BC` (скакалка со счётчиком) и
`ACRB5FR400CL` (карточка-пустышка).

### Снята с производства: ACRB1SK201BC (скакалка со счётчиком)

Владелица сообщила 02.09 вечером. Из разбора исключена: в группе «Скакалки»
три карточки, а не четыре. Вместе с ней ушли из-под нашего предложения
запросы `скакалка с счетчиком` (43 456 показов) и `скакалка спортивная
взрослая с счетчиком` (2 555, наша позиция была 7).

Второй раз за сессию: до этого так же снялась палочка-кость `AAND1ST202BL`
из игрушек. Проверять статус товара **до** разбора, а не после.

### Цвета и уровни у ACRB5FR200CL расходятся между лентой и слайдом

На главном слайде уровень выткан на каждой резинке, на слайде «5 уровней
нагрузки» те же уровни привязаны к другим цветам. Совпадает только чёрная:

| цвет ленты | подписано на резинке | на слайде «5 уровней» |
|---|---|---|
| чёрная | X-HEAVY | X-HEAVY · 45 кг ✓ |
| зелёная | X-LIGHT | **MEDIUM · 27 кг** |
| жёлтая | MEDIUM | **X-LIGHT · 9 кг** |
| синяя | LIGHT | **HEAVY · 36 кг** |
| красная | HEAVY | **LIGHT · 18 кг** |

Четыре из пяти переставлены попарно. Покупатель берёт зелёную как самую
лёгкую — так написано на ленте, — а слайд обещал ей 27 кг. Правится
на слайде, а не в карточке. Прочитано с фотографии, надписи мелкие —
сверить с товаром в руках.

В описании уровни поэтому перечислены без привязки к цвету: пока расхождение
не снято, назвать цвет — значит ошибиться в четырёх случаях из пяти.

### Ответы владелицы 03.09 — три вопроса закрыты

1. **Упаковка игрушек — «пакет с клапаном».** Заполнено у всех шести карточек.
   Остался хвост: у канатов пакет заодно числится отдельной позицией
   в «Комплектации». Комплектация отвечает на «что в коробке», упаковка —
   на «во что вложено»; пакет в обоих полях сразу это дубль. Решение за владелицей.
2. **`ACRB5FR400CL` (латексный набор 5 шт) пока не в продаже** — из разбора
   исключена, в группе «Фитнес-резинки» три карточки. Данных по ней и не было:
   две характеристики из пятнадцати, ноль остатка, ноль заказов, кластеров нет.
3. **Мешки отложены до среза Эвирмы.** Дальше по ним не работаем.

Габариты наборов мешков, дозаполненные 03.09, оставлены как есть: это
заполнение полей предмета, а не пересборка наименований. Соглашение
(«габарит по наибольшему мешку состава») помечено в разборе как требующее
подтверждения — вернёмся к нему вместе со срезом.

### Вес брутто был в базе всё время — моя ошибка

Колонка «Вес товара с упаковкой» стояла пустой по всей книге с пометкой
«нужен замер». Данные при этом лежали в `sku_catalog` в поле
`dimensions.weightBrutto` — заполнены у всех 58 карточек.

Как я ошибся: 02.09 я проверил соседнее поле `unit_weight_kg`, увидел, что
у шести таблетниц оно меньше веса нетто, и заключил, что брутто в каталоге нет.
Поля разные, и `weightBrutto` я не посмотрел вовсе.

Подтверждено независимо: выгрузка кабинета «Общие характеристики одним файлом»
от 02.09 даёт те же цифры во всех 58 строках. Книга заполнена, лист размеров
тоже.

**Конфликт «брутто меньше нетто» реален и лежит в самом кабинете** — у семи
карточек:

| артикул | брутто | нетто | разница |
|---|---|---|---|
| `AAND1RP301BG` | 550 | 580 | −30 |
| `AAND1RP301YL` | 350 | 380 | −30 |
| `ACRA7TB201WH` | 150 | 170 | −20 |
| `ACRA7TB201CL` | 150 | 160 | −10 |
| `ACRA7TB301CL` | 150 | 160 | −10 |
| `ACRA7TB301GR` | 150 | 160 | −10 |
| `ACRA7TB301WH` | 150 | 160 | −10 |

Брутто меньше нетто физически невозможно: либо нетто завышен, либо брутто
занижен. Лист «Размеры и вес» показывает эти строки отдельной пометкой.

### Выгрузки кабинета как канал заливки

Владелица прислала три шаблона WB от 02.09: «Общие характеристики одним
файлом» (26 колонок, 86 карточек), «Артикулы» и «Баркоды». Это те же файлы,
в которые вносят правки и загружают обратно — то есть заливка может идти
не ручным переносом из книги, а заполнением шаблона.

Колонки шаблона: Наименование, Описание, Цвет, Баркод, Вес с упаковкой,
Высота/Длина/Ширина упаковки, ТНВЭД, Ярлыки, Ставка НДС.

Стоит обсудить: генерировать ли из книги готовый файл в формате шаблона.
Сейчас книга — список того, что вносить руками.

### Что реально залито в кабинет на 03.09

Сверка книги с выгрузкой `fetch-wb-content` от 03.09 06:52:

| группа | наименования | описания | вывод |
|---|---|---|---|
| Мячи 6-7-9 см | 13/15 | 13/15 | **залито**, кроме `107BL` и `109LI` |
| Таблетницы 101, 201 | 0/5 | 0/5 | залита **более ранняя редакция** — длины совпадают, начала расходятся |
| Таблетницы 301 | 0/3 | 0/3 | **старые тексты**, 3080–3097 знаков |
| Массажёры 101 | 0/3 | 0/3 | ранняя редакция либо не залито |
| Мешки | 0/12 | 0/12 | не залито |
| Игрушки, скакалки, резинки | 0/12 | 0/12 | не залито — разборы сделаны 02–03.09 |

### Габариты мешков: правило владелицы 03.09

**«По мешкам надо указывать габариты упаковки. Габариты мешков в комплекте
указываем в комплектации».** Моё прежнее соглашение «габарит по наибольшему
мешку состава» отменено.

Применено к пяти наборам: `3BW201WH` и `3BW202WH` — 21 × 15 × 3,
`3BW101BG` — 25 × 20 × 5, `3BW102BG` — 25 × 17 × 4, `7BW101BG` — 30 × 21 × 6.
Цифры из `packaging.json` (выгрузка `sku_catalog`).

**Семь одиночных карточек оставлены как есть — нужен ответ.** Правило упирается
в каркасные контейнеры: они не мягкие, габарит предмета у них осмыслен, а у
`1BW301WH` он ещё и вынесен в наименование («…30 см»). Поставить туда 19 —
значит поссорить заголовок с характеристиками.

**Отдельная находка: контейнеры обуви не влезают в свою упаковку.**
`1BW301WH` — предмет 30 см, упаковка 19 × 17 × 3. `1BW302WH` — предмет 35 см,
упаковка 22 × 22 × 4. Либо контейнер складной и 30 см это раскрытый размер,
либо габариты упаковки в `sku_catalog` неверны. Выяснить до заливки: от этого
зависит и логистика, и что писать в полях.

### Срез Эвирмы по мешкам — какие запросы снимать

Прежний список в этом файле собран по показам и неточен: `мешки для стирки
белья в стиральной машине` даёт 59 055 показов, но **2 заказа** и позицию 69.
По годовому отчёту картина обратная — там, где мы в топ-5, заказы идут при
частотности около тысячи, а на больших запросах мы на 61–93 месте.

| запрос | показов | заказов | позиция | брать в срез |
|---|---|---|---|---|
| `мешки для стирки` | 109 802 | 25 | 61 | **да** — главный запрос категории |
| `мешок для стирки обуви` | 511 394 | 13 | 93 | **да** — самый частотный, отдельная ниша |
| `мешок для стирки большой` | 9 900 | 30 | 16 | **да** — близко к топу, что мешает дойти |
| `сетка для стирки белья в машинке большая` | 5 209 | 24 | 6 | **да** — лучшая позиция на заметной частотности |
| `мешок для стирки колготок капроновых` | 1 013 | 43 | 4 | нет — мы и так четвёртые |
| `мешок для стирки большой 80*90` | 662 | 34 | 2 | нет — мы вторые |
| `мешок для стирки подушек` | 1 046 | 32 | 3 | нет — мы третьи |

### Что ждёт ответа владелицы по трём группам

1. **Фото слайдов скакалок и фитнес-резинок** — конструкция там взята из полей
   кабинета, а не с фото. Перед заливкой описаний сверить.
2. **Упаковка игрушек** — во что кладём мяч и канат. По другим группам ответ
   есть («пакет с клапаном, кроме шпателей»), по игрушкам нет.
3. **Материал шнура и ручек скакалок** — поле «Материал изделия» пустое у всех
   четырёх, а это ключевой признак выбора и рабочий запрос.
4. **Тип питания счётчика `ACRB1SK201BC`** — батарейка сменная или встроенная.
5. **`ACRB5FR400CL` — карточка-пустышка**: две характеристики из пятнадцати,
   ноль остатка, ноль заказов, себестоимость 45 ₽ против 363–487 ₽ у соседей.
   Товар живой или закрываем?
6. **`ACRB3FR100CL` (nm_id 550530967)** есть в годовом отчёте, но нет
   в `sku_catalog`. Архив или сбой синка?
7. **`ACRB1SK201BC` — нулевой остаток при позициях 7–13** по трём запросам
   про счётчик. Самая дорогая потеря: позиции есть, товара нет.
8. **Замер мячей**: «Ширина 8 · Глубина 6» при заявленном диаметре 7 см —
   у круглого `102` глубина не может быть меньше диаметра.


Метод — `docs/seo/card-assembly.md` (§1.2.2 два среза, §П1–§П7 проверки),
канон — `tasks/rules.md` §20. Ход сессии — `tasks/SESSION_LOG.md`, верхняя запись.

### Главное

**Мячи залиты владелицей** — 15 карточек, первая заливка за три сессии.
**Массажёры 101 не залиты**, три карточки на старых текстах.
**Годовой отчёт загружен целиком**: 344 строки, 11 предметов, 2 196 заказов.
Восемь групп больше не заблокированы отсутствием данных.

### Состояние групп

| группа | SKU | заказов за год | состояние |
|---|---|---|---|
| Таблетницы | 10 | 684 | залиты, блок «Честно» убран |
| Мешки для стирки | 12 | 452 | под ключ, ждёт заливки |
| Мячи 6/7/9 см | 15 | 426 | **залиты**, поправить `107BL` |
| Массажёры 101 | 3 | 46 | под ключ, **не залиты** |
| Игрушки для животных | 6 | 190 | не начата, **следующая** |
| Пробки для бутылок | 3 | 89 | не начата |
| Бандажи косметические | 3 | 85 | не начата |
| Скакалки | 4 | 53 | не начата |
| Ножи для пиццы | 2 | 37 | не начата |
| Эспандеры | 4 | 30 | не начата, разобрать аномалию рекламы |
| Шпатели кондитерские | 1 | 27 | не начата, спорный код ТН ВЭД |
| Консервные ножи | 1 | 12 | не начата, ниже порога рентабельности |

### Что делать первым делом

1. **Замер через месяц.** Снять новый период годового отчёта и сверить дельту
   по заказам (`v_search_query_delta`, `docs/seo/measurement.md`). До этого
   эффект пересборки неизмерим. Договорённость владелицы 02.09.
2. Разбор игрушек для животных — данные есть, нужны фотографии товара.
3. Вопросы покупателей и рич-контент из API — оба подтверждены владелицей.

### Задачи в работе (подтверждены владелицей 02.09)

**1. Вопросы покупателей WB → `wb_questions_fact`.** Оценка полтора часа.

Сейчас в базе только отзывы (`wb_reviews_fact`), то есть половина обратной связи.
Отзыв пишут после покупки — это оценка; вопрос задают до неё — это сомнение,
которое мешает купить, и ровно оно должно сниматься в первых 150 знаках описания.
Прецедент: разделение мелкой и крупной сетки у мешков пришло именно из вопросов.
Для игрушек ожидаемо спросят про размер пасти и прочность.

Сделать: таблица по образцу `wb_reviews_fact` (RLS без политик), edge-функция сбора
через Feedbacks API (отдельный метод от отзывов), крон рядом с отзывами, результат
в `public.ingestion_log`, поиск по тексту вопросов как сейчас по отзывам.

**2. Рич-контент из API.** Оценка полтора часа.

Владелица оплачивает опцию «Показ и генерация рич-контента без ограничений»
в конструкторе тарифов, но в `sku_catalog` его нет: `fetch-wb-content` тянет
только характеристики и описание. Разбирать рич-контент вслепую нельзя.
Столбец под него в книге уже есть — пустой.

Сделать: разведка контракта через `probe-wb-api` (форма ответа неизвестна),
затем сбор рядом с `fetch-wb-content`.

Обе задачи не зависят ни от Эвирмы, ни от фотографий — делаются, пока группы ждут.

### Третий источник: Промопульт (файл 02.09)

Владелица прислала выгрузку Промопульта «Кластеры: сравнение позиций» —
1 303 строки, 134 блока. Это **ключи**: наши карточки с привязанными кластерами
и частотностью. **Не замена второму срезу** — конкурентов, цен и доли рекламы нет.

Что даёт сверх Джема: кластеры, по которым у нас **нет заказов**. Годовой отчёт
выгружается с фильтром «Заказывали», поэтому спрос без продаж в него не попадает
вообще. По игрушкам это, например: «мячик для собак» 25 185, «резиновые игрушки
для собак» 4 127, «игрушка для зубов собаке» 1 608, «неуловимое яйцо для собак»
1 575, «антивандальная игрушка для собак» 1 330, «жевательная игрушка» 1 353.

**Позиций в файле нет** — подтверждено владелицей. Значит источник отвечает
на вопрос «что ищут», но не на «где мы стоим». Сравнивать его частотности
с частотностями Джема нельзя (§П4, разные источники).

### Эвирма: по мешкам среза НЕТ — снять

Проверка 02.09 по `wb_serp_snapshots`: в базе девять запросов, все от 27.08 —
массажёры (четыре), мячи (два) и таблетницы (три). **Мешков среди них нет.**

Это уточняет запись SESSION_LOG от 28.08, где мешки числятся «под ключ
по двум срезам». Второго среза по ним не снимали: разбор делался на годовом
отчёте и отзывах. Наименования собраны по заказам и остаются верными, но
решения вида «идти ли в новый запрос» по мешкам принимались вслепую — ровно
то, от чего предостерегает §1.2.2.

**Снять срез по запросам, на которых мешки реально продают** (из годового отчёта):

| запрос | показов | заказов |
|---|---|---|
| `мешки для стирки белья в стиральной машине` | 59 055 | 2 |
| `сетка для стирки белья в машинке` | 46 688 | 10 |
| `сетка для стирки белья в машинке большая` | 5 209 | **24** |
| `пакет для стирки белья сетка` | 0 в отчёте | **10** |

Последний примечателен: десять заказов при нулевой частотности в выгрузке —
запрос есть, а показов отчёт не приводит. Полка покажет, кто там стоит.

### Решение 02.09: игрушки для животных — состав группы и конструкции

**Палочка-кость с зубной щёткой внутри (`AAND1ST202BL`) снята с производства.**
Из разбора исключена: в группе шесть карточек, 190 заказов за год, а не семь и 196.

По фотографиям владелицы зафиксированы конструкции — до разбора, чтобы не
достраивать их потом по отзывам и габаритам:

| артикул | конструкция | заказов | остаток |
|---|---|---|---|
| `AAND1BL102RD` | мяч **круглый, протектор**, красный, ремешок 29 см | 64 | 111 |
| `AAND1BL102BL` | то же, синий | 43 | 129 |
| `AAND1BL101RD` | мяч **регби, шипы**, красный, ремешок 28 см | 38 | 117 |
| `AAND1BL101BL` | то же, синий | 28 | 158 |
| `AAND1RP301BG` | канат **замкнутой петлёй**, хлопок, 55 × 5 см | 9 | **6** |
| `AAND1RP301YL` | канат **прямым жгутом с кистями**, хлопок, 55 × 5 см | 8 | **10** |

Различие внутри пар — не в размере собаки, а в форме и поверхности: круглый
с протектором против регби с шипами, петля против прямого жгута. Диаметр мяча
у обеих пар одинаковый — 7 см. В наименованиях сейчас ни формы, ни шипов,
ни протектора, ни петли.

Два вопроса к разбору:

1. **«Пуллер палка для собак»** — запрос привязан к `301YL`, прямому жгуту,
   2 заказа с конверсией 100%. Пуллер — это кольцо, а кольцо у нас `301BG`.
2. **Остатки канатов кончаются** — 6 и 10 штук против 111-158 у мячей.
   Прежде чем переписывать, выяснить, будет ли закупка.

### Ждут решения владелицы

| что | цифра |
|---|---|
| Фото цветов мячей: `PN`×3, `106WH`, `107WH` | — |
| Шпатели: 3924 (список) или 3926 (матрица) | 3924 попадает в проект маркировки |
| `106RJ` и `109BС` без «мфр», хотя на «мфр» зарабатывают | 3 и 2 заказа |
| `109PN`: топ-запрос «мячики для мелкой моторики» — стоп-лист | 4 заказа, позиция 17 |
| Детские запросы у таблетниц — стоп-лист, довод за код 9503 | 7 заказов |
| `401BC` — возвращать ли закупку | 3 заказа, остаток 0 |
| `ACRA7TB101WH` — существует ли карточка | нет в каталоге, ноль строк в отчёте |
| Диаметр обувного мешка XL | — |
| Консервные ножи и прессы для чеснока — кода ТН ВЭД нет | отложено «на потом» |

### Решение 02.09: коды ТН ВЭД — по матрице владелицы

Прислана матрица «ТН ВЭД × ОКПД 2» на 103 SKU и 23 группы, выбранный код помечен
зелёным. Источник — сама владелица, не декларации. Семь групп из одиннадцати
разошлись с прежней проработкой; разбор и цена расхождений — `docs/marking/codes-marking.md`.

Главное: мячи и массажёры переведены с 9019 на **9506**. Обязательная маркировка
«Честный ЗНАК» с 01.10.2026 к ним больше не относится. Риск: код спортивный,
а карточки остались в предмете «Массажеры механические» с «массажный мяч»
в наименовании и «снятие отечности» в поле «Действие».

### Решение 02.09: цвет не трогаем

Розовый — один цвет, белый — один. «Слоновая кость», «айвори», «молочный»,
«бежевый» — ноль вхождений в обоих срезах. Цвет **вообще** не встречается
в поисковых запросах: единственные совпадения на «бел» — это «стирка белья».
Поле «Цвет» работает как фильтр каталога, не как поиск.

### Экономика: расходы, которых не видно в марже

Выручка упала в шесть раз за полгода: март 578 288 ₽ → август 91 009 ₽.
При этом Джем (22 990 ₽/мес) и минимальный платёж конструктора тарифов
(5 000 ₽/мес) **списываются с баланса, а не через отчёт реализации** —
в `wb_reports_fact` их нет, и маржа в сводке их не учитывает.

Реклама в отчёт попадает, но с `nm_id = 0`: за неделю 24–31.08 она составила
6 757 ₽ при прибыли от продаж 6 712 ₽. Теперь выводится отдельным блоком сводки.

Проверить окупаемость: эспандеры дали 14 заказов с 218-й позиции — это реклама.

---

## [устарело] 2026-08-28 (вечер) — Четыре группы пересобраны по заказам, заливки по-прежнему нет

Метод — `docs/seo/card-assembly.md` (§1.2.2 два среза, §П1–§П7 проверки),
канон — `tasks/rules.md` §20. Ход сессии — `tasks/SESSION_LOG.md`, верхняя запись.

### Главное

**Ничего не залито в кабинет.** Сорок карточек в четырёх группах готовы под ключ
в репозитории и живут на старых текстах. Это единственное, что отделяет работу
от результата, и оно не сдвинулось за две сессии.

Книга для заливки — `Карточки АРОЛС 2026-08-28.xlsx`, 40 карточек, шесть листов,
инфографика проставлена у всех сорока.

### Состояние групп

| группа | SKU | заказов за год | состояние |
|---|---|---|---|
| Таблетницы | 10 | 684 | под ключ по двум срезам, ждёт заливки |
| Мешки для стирки | 12 | 452 | под ключ по двум срезам, ждёт заливки |
| Мячи 6/7/9 см | 15 | 426 | под ключ, наименования по заказам |
| Массажёры 101 | 3 | 46 | под ключ, наименования по заказам |
| Игрушки для животных | 7 | 196 | не начата, **следующая** |
| Пробки для бутылок | 3 | 89 | не начата |
| Бандажи косметические | 3 | 85 | не начата |
| Скакалки | 4 | 53 | не начата |
| Ножи для пиццы | 2 | 37 | не начата |
| Эспандеры | 4 | 30 | не начата |
| Шпатели кондитерские | 1 | 27 | не начата |
| Консервные ножи | 1 | 12 | не начата, срез не покупать |

### Что делать первым делом завтра

1. **Срезы Эвирмы** по оставшимся группам — список из 14 запросов в блоке
   «Решение 28.08: Эвирма» ниже.
2. **Загрузить оставшиеся 141 строку** годового отчёта: в `wb_search_queries`
   лежат три предмета из одиннадцати. Подробности — в блоке «Остальные группы
   годового отчёта» ниже.
3. Разбор по убыванию заказов, начиная с игрушек для животных (196).

### Ждут решения владелицы

| что | цифра |
|---|---|
| `106RJ` и `109BС` без «мфр», хотя на «мфр»-запросах зарабатывают | 3 и 2 заказа |
| `109PN`: топ-запрос «мячики для развития мелкой моторики рук» — стоп-лист | 4 заказа, позиция 17 |
| Детские запросы у таблетниц — стоп-лист, довод за код 9503 | 7 заказов |
| `401BC` — возвращать ли закупку | 3 заказа, остаток 0 |
| `ACRA7TB101WH` — существует ли карточка | нет в каталоге, ноль строк в отчёте |
| Диаметр обувного мешка XL | — |
| Фото цветов мячей: `RJ`×3, `PN`×3, `106WH`, `107WH` | — |
| Четыре спорных отзыва по мешкам | — |

---

### Решение 28.08: подгруппа мячей 103 — дораспродаём

Выведена из продажи 27.08, остаток 82 шт. на трёх карточках. Годовая выгрузка
показала, что за год она дала **59 заказов** по своим запросам: «мяч для мфр
жесткий» 3 302, «мячик массажный жесткий гладкий» 289. Гладкие мячи ищут
отдельно от шипованных.

Решение владелицы: **дораспродаём, время на неё не тратим.** Цифры записаны
в `docs/seo/fill-plan/massage-balls-106-107-109.md` §12 на случай, если вопрос
встанет заново.

### Решения 28.08 по мячам: два слова возвращены в оборот

| слово | частотность | заказов за год | где ставим |
|---|---|---|---|
| **лимфодренажный** | 12 146 | 46 | `106PN`, `106WH`, `106BС` |
| **ортопедический** | 5 046 | 46 | **только** `109RJ` и `107RJ` |

Оба были в стоп-словах. Оба описывают применение, а не исход, — граница
и обоснование в `docs/seo/stop-words.md`, раздел «Разрешено с оговоркой».

«Ортопедический» ограничен двумя карточками сознательно: прилагательное —
довод за позицию ТН ВЭД 9021 «приспособления ортопедические», а мячи держим
в спортивной 9506. Код от текста карточки не меняется, но WB может запросить
регистрационное удостоверение на карточку, заявленную как ортопедическое
изделие. Все 46 заказов и так сосредоточены на двух оранжевых.

Третье, уже не вопрос, а находка: **номера артикулов на слайде «различные
цвета» дали 121 заказ за год.** Прежняя рекомендация снять их неверна,
поправлено в `docs/seo/infographics/myachi-106-107-109.md`.

---

### Задача: подключить вопросы покупателей WB

Владелица использует раздел «Вопросы покупателей» как рабочий источник — оттуда
пришло разделение мелкой и крупной сетки у мешков (мелкая держит пух и шерсть,
чтобы не забивали барабан). В базе вопросов нет: `wb_reviews_fact` грузит только
отзывы, вопросы WB отдаёт отдельным методом Feedbacks API.

Из-за этого разбор идёт вслепую по половине обратной связи. Отзыв пишут после
покупки, вопрос — **до неё**, и это прямая речь сомневающегося покупателя:
ровно то, что должно попадать в первые 150 знаков описания.

Сделать:
1. Таблица `wb_questions_fact` — по образцу `wb_reviews_fact`, RLS без политик.
2. Edge-функция сбора, крон рядом с отзывами, результат в `public.ingestion_log`.
3. Поиск по тексту вопросов — как сейчас по отзывам, для разбора групп.

Оценка: полтора часа. Приоритет — до следующей группы, которая пойдёт «под ключ».

---


### Задача: автосбор годового отчёта поисковых запросов через официальный API

Отчёт «Поисковые запросы по вашим товарам» владелица выгружает вручную в xlsx
и присылает zip'ом. Он лежит в основе всей пересборки наименований: за 28.08
по нему переписаны четыре группы. Забирать его можно официальным API — тем же,
за который уже платится подписка «Джем».

Метод: `POST /api/v2/search-report/report`, хост `seller-analytics-api.wildberries.ru`,
раздел аналитики продавца, доступен только с подпиской «Джем». Отдаёт **только наши
номенклатуры** — чужую полку официальный API не отдаёт вообще, второй срез
остаётся за платным сервисом (см. решение по Эвирме ниже).

Сделать:
1. **Разведка контракта** через `probe-wb-api` — точная форма запроса и ответа
   неизвестна: `dev.wildberries.ru` заблокирован egress-прокси, писать маппинг
   по памяти нельзя. Сырой ответ ложится в `ingestion_log.meta.probe`.
2. Функция `fetch-wb-search-queries` по образцу `fetch-wb-funnel`: `checkCronSecret`,
   `WB_TOKEN_READ`, запись в `ingestion_log`, upsert в `wb_search_queries`
   по ключу `(period_from, period_to, nm_id, query)`.
3. Крон — раз в месяц. Каждый прогон = новый период, старые остаются:
   на этом стоит `v_search_query_delta` и весь замер (`docs/seo/measurement.md`).

Оценка: два часа, из них час на разведку контракта.
**Остановлено владелицей 28.08 на шаге 1 — «пока стоп, не торопимся».**
Возобновлять по явному слову.

### Решение 28.08: парсинг публичной выдачи WB запрещён

Рассматривался бесплатный способ снимать чужую полку — публичный поисковый
эндпоинт WB (`u-search.wb.ru/.../v18/search`), он отдаёт цену, рейтинг, отзывы
по каждой позиции топ-100. **Владелица запретила: риск бана аккаунта.**
У аккаунта уже есть T&S-флаг (Actions отключены с 11.07), рисковать им нельзя.

Правило: **только официальный API продавца.** Оно уже записано в шапке
`supabase/functions/probe-wb-api/index.ts` — «Строго только официальный API…
Никакого парсинга публичной выдачи». Соблюдать во всех репо.

### Решение 28.08: Эвирма — оплатить один месяц

Официальный API чужую полку не отдаёт, бесплатных замен нет (у Stat4Market
и Wildbox бесплатно только позиции своих товаров), парсинг запрещён. Значит
второй срез покупается.

План месяца: снять срезы **сразу по всем оставшимся группам**, затем отменить
подписку. Срезы лягут в `wb_serp_snapshots` и будут доступны сколько угодно долго.

| группа | заказов за год | что снимать |
|---|---|---|
| Игрушки для животных | 196 | «игрушки для собак крупных пород» (120 163, поз. 58) · «игрушки для собак» (883 484, поз. 23) · «мяч на веревке для собак» (6 135) |
| Пробки для бутылок | 89 | «крышка для вина бутылки» (2 164, поз. 8) · «пробка для вина» (48 172, поз. 58) |
| Бандажи косметические | 85 | «бандаж для лица» (166 781, поз. 77) · «бандаж для челюсти» (2 567, поз. 10) |
| Скакалки | 53 | «скоростная скакалка» (12 518, поз. 65) · «скакалка для фигурного катания» (3 146, поз. 15) |
| Ножи для пиццы | 37 | «нож для пиццы» (204 358, поз. 51) |
| Эспандеры | 30 | «резинка для фитнеса тканевая» (20 563, поз. 79) |
| Шпатели кондитерские | 27 | «скребок для теста» (12 980, поз. 12) |
| Консервные ножи | 12 | не снимать — срез дороже группы |

### Две аномалии из годового отчёта, ждут проверки

1. **Эспандеры.** «Фитнес резинка тканевая» — 4 207 показов, **позиция 218
   и 14 заказов**. С 218-й позиции органика столько не даёт: почти наверняка
   реклама. Проверить ставку — возможно, группа убыточна.
2. **Шпатели кондитерские.** К карточке привязан запрос **«ведро складное 3 л»**,
   15 462 показа, 1 заказ. Чужой товар. Либо в тексте есть слово, которого там
   быть не должно, либо ошибка привязки на стороне WB.

### Остальные группы годового отчёта — не загружены в базу

В `wb_search_queries` лежат три предмета из одиннадцати: таблетницы (48 строк),
мешки (60), «Массажеры механические» (115 — мячи и массажёры вместе).
Оставшиеся 141 строка по восьми группам в базу не внесены; SQL на вставку
собирается из xlsx скриптом, файл отчёта — в загрузках сессии.

---

### Рабочее окружение сессий (закреплено 2026-08-28)

| | значение |
|---|---|
| окружение | **Arols** — в `Default` нет setup script, там вернутся окна на каждый шаг |
| модель | **Sonnet 5** по умолчанию, Opus 5 или 4.7 на разборы |
| нельзя | **Haiku 4.5** — на нём отключается режим `auto` |
| окна подтверждения | остались только на запросах к базе, жать **Ctrl+Enter** |

Окна на `Execute SQL` — **ожидаемое поведение**, а не поломка: `auto` это режим
с классификатором. Не дополнять allowlist, не менять `defaultMode`, не пересобирать
снапшот — всё испробовано. Разбор и три неверных диагноза — `AI_OS/docs/rules/core/web-permissions.md`.

Осталось: заархивировать окружение `Default` после пары спокойных сессий в `Arols`.

---

## [устарело] 2026-08-28 (день) — Пересборка карточек: три группы готовы, заливки нет

Метод и состояние по каталогу — **[`docs/seo/fill-plan/README.md`](../docs/seo/fill-plan/README.md)**.
Ход сессии — `tasks/SESSION_LOG.md`, верхняя запись. Правило единицы товара — `tasks/rules.md` §18,
регламент пересборки — `docs/seo/card-assembly.md` §19.

### Главное

**Ничего не залито в кабинет.** Три группы готовы под ключ в репозитории, все 32 карточки
живут на старых текстах. Разрыв между «написано» и «стоит на витрине» — сейчас
единственное, что отделяет работу от результата.

### Живые блокеры на витрине, по убыванию

1. **Склейка через запятую** в поле «Действие» у `ACRB1MS101BL` и `ACRB1MS101PN`:
   «восстановление, реабилитация, оздоровление» и «снятие мышечного напряжения,
   отечности, улучшение кровообращения, сна». С 01.05.2026 — основание для теневого
   бана до 30 дней. Две карточки, правится за минуты.
2. **Риск R до шести** у мячей: «беременность», «противопоказания», «лечебный»,
   «реабилитация», «акупунктурный», «логопедический», «новорожденные», «для малышей»,
   «игрушка», «сенсорный», «мелкая моторика». Хуже всех `106PN` и `106WH`.
3. **Три массажёра с одинаковыми зонами** `спина · шея · плечи` — §18, карточки
   конкурируют между собой.
4. **Таблетницы** — «детск», «медицинск», «подростк» на восьми карточках из десяти.

### Порядок групп

| группа | SKU | состояние |
|---|---|---|
| Массажёры роликовые ACRB1MS101 | 3 | **готово под ключ** + задание на инфографику, ждёт заливки |
| Массажные мячи 6 / 7 / 9 см | 15 | **готово под ключ** + задание на инфографику, ждёт заливки |
| Таблетницы ACRA7TB | 10 | **готово под ключ** + задание на инфографику, ждёт заливки |
| Мешки для стирки | 12 | материал собран, фото получены, **ждёт привязки артикул ↔ фото** |
| Игрушки для животных | 7 | не начата |
| Скакалки · Эспандеры | 4 + 4 | не начаты |
| Бандажи косметические | 3 | не начата, самый высокий негатив в отзывах |
| Пробки, ножи, шпатели, консервный нож | 8 | не начаты |
| Капы | 8 | **не трогаем** по решению владелицы |

### Что нужно от владелицы

- Мячи: фото цветов `RJ` во всех трёх диаметрах, `PN` во всех трёх, `106WH`, `107WH`.
  Цвет не подтверждён, а в отзывах есть претензия «цвет не соответствует карточке».
- Мячи: подгруппа `103` выведена из продажи, но остаток 82 шт. и живые карточки
  с риском R — снимаем совсем или дораспродаём.
- Таблетницы: лист наклеек `1–7` / `SUN–SAT` в комплекте или нет; подтвердить строку
  про крепления у трёх карточек `101*`; судьба `ACRA7TB401BC`; почему `ACRA7TB101WH`
  нет в каталоге карточек.
- Мешки: привязка артикул ↔ фотонабор.

### Инструменты, появившиеся в этой сессии

- `wb_product_facts`, `wb_card_slides`, `v_card_slide_issues` — факты с фотографий
  и содержимое слайдов больше не теряются вместе с контекстом чата.
- `wb_serp_snapshots` — девять запросов, база для монитора конкурентов.
- `scripts/build_cards_xlsx.py` — книга для заливки собирается из файлов описаний.
- `scripts/build_group_artifact.py` — один сводный артефакт на группу из markdown репо.

---

### Правило метода, добытое ошибкой 28.08

**«Текущее наименование» брать из `sku_catalog`, а не из файла описаний.**

Я заявил, что «мфр» нет ни в одном наименовании мячей и это главный резерв
группы. Слово стоит во всех пятнадцати. Ошибка в источнике: таблица
«Наименования» в `docs/seo/descriptions/*.md` — это **предложения**,
а живое состояние лежит в `sku_catalog.title`.

То же касается любых утверждений «сейчас стоит X»: проверять запросом,
а не по своему же файлу.

---

## [устарело] 2026-08-27 — Пересборка карточек: тексты

Метод и состояние по каталогу — **[`docs/seo/fill-plan/README.md`](../docs/seo/fill-plan/README.md)**.
Ход сессии — `tasks/SESSION_LOG.md`, верхняя запись. Правило единицы товара — `tasks/rules.md` §18.

### Постановка

Карточки заполнялись три года назад, алгоритмы ранжирования с тех пор изменились.
Перебирается **вся матрица целиком**, а не только пустые поля: заполненное значение
не считается верным, пока не проверено. Сейчас тексты, следующий этап — фотографии.

### Порядок групп

| группа | SKU | состояние |
|---|---|---|
| Массажёры роликовые ленточные ACRB1MS101 | 3 | **готово под ключ**, ждёт заливки в кабинет |
| Массажные мячи 6 / 7 / 9 см | 19 | материал собран, **ждёт фотографий** |
| Мешки для стирки | 12 | материал собран, **ждёт фотографий** |
| Таблетницы | 10 | не начата |
| Игрушки для животных | 7 | не начата |
| Скакалки · Эспандеры | 4 + 4 | не начаты |
| Бандажи косметические | 3 | не начата, самый высокий негатив в отзывах |
| Пробки, ножи, шпатели, консервный нож | 8 | не начаты |
| Капы | 8 | **не трогаем** по решению владелицы |

### Два правила метода, добытые дорого

1. **Перед разбором группы — фотографии товара.** Домысел о конструкции роликового
   массажёра стоил трёх неверных выводов, включая выдуманный блокер.
2. **Отсутствие кластера в выгрузке — не доказательство отсутствия спроса.**
   `wb_keyword_clusters` замкнута на то, что в карточках уже написано.

### Ближайшее

- Залить комплект по роликовым массажёрам. Первым делом — «Действие» у голубого
  и розового: склейка значений через запятую, с 01.05.2026 риск теневого бана.
- ~~Фотографии мячей~~ — получены 27.08 по 106, 107 и 109; 103 выведена из продажи.
- ~~Детский интент у мячей~~ — **решено 27.08: отсекаем.** Пять кластеров, около 25 500
  частотности. Со слайда инфографики убрать «развивает мелкую моторику».
- Дефект мячей «разваливается по шву» и запах пластика — к поставщику.
- **Мячи: комплект под ключ готов** — наименования, поля и 15 описаний в
  [`docs/seo/descriptions/myachi-106-107-109.md`](../docs/seo/descriptions/myachi-106-107-109.md).
  Порядок заливки: поля → наименования → **инфографика** → описания.
- ~~Мячи: залить значения полей~~ — вошло в комплект выше. Прежняя формулировка:
  [`docs/seo/descriptions/myachi-polya-106-107-109.md`](../docs/seo/descriptions/myachi-polya-106-107-109.md):
  зона массажа по диаметру, действие (убрать «упругость» у шести карточек),
  материал `ПВХ`, цвет по трём проверенным карточкам.
- **Мячи: фото розового, белого и оранжевого** — по ним поле «Цвет» не проверено.
- **Мячи: подтвердить зоны массажа по диаметру** — предложение в
  [`docs/seo/fill-plan/massage-balls-106-107-109.md`](../docs/seo/fill-plan/massage-balls-106-107-109.md) §10.
  Для 7 и 9 см это гипотеза, проверяется свежей выгрузкой через месяц после правки.
- ~~Мячи: фотография подгруппы 103~~ — **снято 27.08: подгруппа выводится из продажи.**
  Остаток 82 штуки распродаётся как есть, тексты не трогаем.
- **Мячи: инфографика идёт вместе с текстами, а не после.** На слайдах напечатаны
  «фасцит, плоскостопие», «борется с целлюлитом», «рефлексотерапия», «мелкая моторика» —
  модератор читает их наравне с описанием. Слайды общие: один набор закрывает 15 карточек.
- Фотографии мешков для стирки — семь позиций, список в
  [`docs/seo/fill-plan/meshki-stirki-1005.md`](../docs/seo/fill-plan/meshki-stirki-1005.md) §9.
- Решить по мешкам: берём ли подарочный интент («бокс для девушки» 17 463 +
  «бокс для подруги» 4 727) и подтвердить отсечение ключей со словами «чехол»
  и «контейнер» (5 198 частотности).
- Дефекты мешков — к поставщику: бегунок молнии и осыпание внутреннего слоя
  (ворсинки на белье). Плюс вопрос по зип-лок упаковке наборов.

---

## [устарело] 2026-08-23 — Кризис складов WB: приоритеты пересобраны

Полный разбор ситуации — **[`docs/WB_SKLADY_2026.md`](../docs/WB_SKLADY_2026.md)**.
Прочитать перед любой работой с остатками, закупкой и карточками. Ход сессии —
`tasks/SESSION_LOG.md`, верхняя запись.

### 🔴 Что изменилось в самой постановке задач

Удары БПЛА по складам WB в июле–августе уничтожили часть сети. С 15.08 новые правила
FBO: склад не выбирают, названия складов убраны из остатков, остался один виртуальный.

**Главное следствие для системы:** «лежит на складе» и «можно продать» больше не одно
и то же. Из 8 033 единиц **5 567 заморожено** на пострадавших складах (844 313 ₽),
доступно **2 466** (349 506 ₽). Не работает 70,7% капитала. Любой расчёт оборотных
средств, дефицита и оборачиваемости без этого различия неверен.

### ⚠️ Два действия, которые ИИ делать не может — напоминать каждый раз

Аккаунт GitHub под флагом T&S: **через API его не беспокоить.** Мерж PR и деплой фронта
только руками владелицы. Канон — `llm_wiki/wiki/workflow.md` § «Обязанность ИИ:
напоминать про мерж и деплой».

Деплой из корня репозитория, не из домашней папки:
`powershell -ExecutionPolicy Bypass -File scripts/deploy.ps1`

### Могу сам — в этом порядке

- [ ] 🔴 **Приём остатков.** `fetch-wb-stocks` считает служебные строки WB
      (`Всего находится на складах`, `В пути…`) настоящими складами — итог складывается
      сам с собой, выходит 17 604 вместо 8 033. Отсеять по явному списку, товар в пути
      хранить отдельно. **Историю не переписывать** — она первоисточник и доказательство,
      считать поверх неё через view. От этого зависят все остальные цифры
- [ ] 🔴 **Вес.** `unit_weight_kg` пуст у 38 из 79 SKU, при том что вес есть
      в `dimensions.weightBrutto`. Юнит-экономика по половине каталога считается без веса
- [ ] 🟠 **Юнит-экономика FBO против FBS** по каждой группе — после веса,
      иначе сравнивать нечем
- [ ] 🟠 **Проверка дефицита:** смотреть все активные SKU, а не топ-20. Закончившийся
      товар из топ-20 выпадает — метрика слепнет ровно тогда, когда должна кричать
- [ ] 🟢 **Автоснимки контента** раз в неделю, сейчас разовый
- [ ] 🟢 **Проверить гипотезу географии** по показам таблетниц (см. `WB_SKLADY_2026.md` §5)
- [ ] 🟢 **Таблица частотностей + view покрытия** — когда владелица пришлёт файл

### За владелицей

- [ ] 🔴 **Проверить «Удержания»** в ЛК WB. Добровольные выплаты идут с 22.07,
      начисляются сами, документов не требуют. Претензия — отдельная вещь, её не подаём
- [ ] 🔴 Раздел A чеклиста — 9 правок, маркировка, срок 01.10.2026
- [ ] 🟠 **Заливка 39 описаний — сейчас, не дожидаясь поставки.** Карточке нужно время
      переиндексироваться, продаж почти нет, риск уронить конверсию нулевой
- [ ] 🟠 Раздел C чеклиста — **поднят в приоритете**: на FBS незаполненные габариты
      умножают логистику в 25 раз, заниженные дают штраф до 10 000 ₽
- [ ] 🟠 Раздел B — характеристики
- [ ] 🟠 Сбор частотностей: ключ + число + группа/артикул + источник и дата

### Решения, принятые 23.08 — не переспрашивать

- **Претензию к WB не подаём** — нет закупочных документов
- **Переход на FBS** через ~4 недели, когда приедет товар. Придут все артикулы
- **Карточки правятся руками в кабинете**, через API не заливать
- **Направление обмена только WB → программа.** Запись на WB не делаем никогда
- **Второй срез «до» не нужен** — снимок от 22.08 свежий. Точка «после» снимается,
  когда вернутся остатки

### Хвосты

- [ ] ⚠️ **РЕПО ↔ ПРОД: `telegram-alerts`.** В проде v23, в репо на коммит больше —
      `margin_pct` умножался на 100. Ветка редкая. Деплоить при следующей правке
      этой функции, отдельный деплой ради этого не стоит риска
- [ ] 🟠 `X-Cron-Secret` нет у 10 функций, включая продажи и заказы
- [ ] 🟠 `sync-sheets` падает на Google с 13.06
- [ ] 🟢 `config.toml` не знает семи функций

### Закрыто 23.08

- [x] Установлена причина падения продаж — не SEO и не конверсия, а физическая потеря складов
- [x] Посчитан замороженный капитал: 844 313 ₽, разбивка по складам, группам, артикулам
- [x] Отменена гипотеза по таблетницам — опиралась на раздутый остаток
- [x] Габариты проверены под FBS: заполнены везде, всё влезает в ограничения ПВЗ
- [x] Подтверждены точки «до»: контент от 22.08, год показов 22.08.2025–22.08.2026

### Отложено осознанно

Поставки (405), реклама (крон отключён), автоматизация заливки карточек,
переход pnpm 9 → 11, балл карточки (до появления ядра).

---

## [устарело] 2026-08-22 (вечер) — конец сессии: карточки за владелицей, техдолг за мной

Полный разбор — `tasks/SESSION_LOG.md`, верхняя запись.

### ⚠️ Два действия, которые ИИ делать не может — напоминать каждый раз

Аккаунт GitHub под флагом T&S (тикет #4535795): **через API его не беспокоить.**
Мерж PR и деплой фронта — только руками владелицы. Канон и формулировки —
`llm_wiki/wiki/workflow.md` § «Обязанность ИИ: напоминать про мерж и деплой».

**Мерж.** Пока PR открыт, изменений в `main` нет — следующая сессия их не увидит.
Кнопкой в веб-интерфейсе, не через API.

**Деплой фронта.** Смерженный PR до сайта сам не доезжает. Из корня репозитория:

```
powershell -ExecutionPolicy Bypass -File scripts/deploy.ps1
```

Не из домашней папки: `vercel deploy` привязывается к текущей директории и заводит
новый проект по её имени (так появился мусорный `arols`).

### Первое по порядку — карточки, за владелицей

- [ ] 🔴 Раздел A чеклиста кабинета — 9 правок. **Не SEO, а классификация и маркировка,
      срок 01.10.2026.** Первым `AAND1ST202BL`: «зубная щётка» в трёх полях
- [ ] 🟠 Заливка 39 описаний из `docs/seo/descriptions/`. Начинать с худших:
      капы (479 знаков на восемь карточек), эспандеры, таблетницы
- [ ] 🟠 Раздел B — характеристики. Важнее описаний: у кап заполнено одно поле на восемь
- [ ] 🟠 Раздел C — 15 правок. Часть про деньги: вес пробок 0,5 кг вместо 0,05 кг
- [ ] После заливки прогнать `docs/seo/scan-stop-words.sql`, **оба запроса**

Порядок и детали — `docs/seo/README.md`.

### Мелочи за владелицей

- [ ] 🔴 Переименовать `.claude/settings.json.example` → `.claude/settings.json`
- [ ] 🟢 Удалить мусорный проект `arols` в панели Vercel
- [ ] 🟢 Смержить открытые PR: SellerBase (эта ветка), LLM_Wiki и AI_OS — канон
      ручного мержа и деплоя

### Могу сам

- [ ] ⚠️ **РАСХОЖДЕНИЕ РЕПО ↔ ПРОД: `telegram-alerts`.** В проде v23, в репо на коммит
      больше — `margin_pct` умножался на 100 (в списке «топ-3 SKU где маржа упала»
      показал бы 500 п.п. вместо 5). Ветка редкая: срабатывает только при падении общей
      маржи больше чем на 15 п.п. **Деплоить при следующей правке этой функции** —
      отдельный деплой ради этого не стоит риска: файл 35 КБ, передаётся вручную,
      опечатка ломает ежедневную сводку
- [ ] 🟠 `X-Cron-Secret` нет у 10 функций, включая продажи и заказы
- [ ] 🟠 `sync-sheets` падает на Google с 13.06
- [ ] 🟢 Регулярные снимки контента — сейчас разовый
- [ ] 🟢 `config.toml` не знает семи функций
- [ ] 🟢 Два `WARN` линтера Supabase, оставлены осознанно: `pg_net` в схеме `public`
      (перенос рискует порвать cron-задания, которые через него дёргают Edge Functions)
      и недоступная на Free проверка утёкших паролей

### Ждёт решения владелицы

- [ ] **Разбор пяти таблетниц** с просевшей конверсией — товар на складе, трафик идёт,
      продажи встали. По моей оценке сейчас ценнее остального техдолга
- [ ] **Семантическое ядро** — выбрать источник: WB Jam / Evirma / разбор 6996 отзывов
- [ ] **Балл карточки** — только после ядра, отложено осознанно

### Закрыто в этой сессии

- [x] Вкладка «SEO карточек» на общих компонентах + блок в карточке товара (PR #221)
- [x] Дефект воронки: WB переименовал `addToCartCount` → `cartCount`. Исправлено,
      год данных восстановлен из выгрузки кабинета
- [x] Деплой фронта разморожен — `scripts/deploy.ps1`, первый успешный за два месяца
- [x] Зомби-задачи: часовой чистильщик + `v_job_health`; `telegram-alerts` починен
- [x] RLS закрыт, ERROR-уровень линтера Supabase пуст (PR #232)
- [x] Снята устаревшая задача по оборачиваемости — WB удалил эндпоинт, считается
      из `v_turnover_by_sku`

### Отложено осознанно

Поставки (405), реклама (крон отключён), автоматизация заливки карточек,
переход pnpm 9 → 11.

---

## [устарело] 2026-08-22 — Задачи после паузы: что за кем закреплено

Полный разбор сессии — `tasks/SESSION_LOG.md`, запись от 2026-08-22.
Здесь только то, что осталось сделать, с разделением по исполнителю.

### 🔴 Деплой фронта — только вручную, пока висит флаг GitHub

**Это не новая проблема.** Уже зафиксировано в `AI_OS/MEMORY/tasks/cross-repo-todo.md`
и `llm_wiki/wiki/workflow.md`: флаг T&S на аккаунте `Arsid0305` (тикет #4535795,
с июля) разом выключил Actions во всех репо, автодеплой Vercel и Supabase GitHub
Integration. Kino-app по той же причине стоял с 26 июня — **та же дата, что
и SellerBase**.

Проверено 22.08: смерженный PR #225 не вызвал сборку. В панели Vercel
«Ошибка: Ссылка на проект не найдена». Поставить приложение Vercel обратно
не выйдет — OAuth-приложения GitHub заблокированы флагом.

**Решение — как в Kino-app: `scripts/deploy.ps1`, одна команда.**

```
powershell -ExecutionPolicy Bypass -File scripts/deploy.ps1
```

Скрипт тянет свежий `main`, ставит зависимости, гоняет typecheck и lint,
подтягивает переменные окружения из Vercel, собирает и выкатывает в прод.
Первый запуск потребует `vercel login` — **входить по email, не через GitHub.**

**Следствие, о котором нельзя забывать:** любая работа над интерфейсом
не доходит до сайта, пока кто-то не запустит скрипт. Сайт при этом выглядит
живым, потому что данные читает из Supabase напрямую — устаревает только код.
Именно так полтора месяца никто не замечал отставания в Kino-app.

**Урок в копилку:** перед диагностикой инфраструктуры сначала читать
`cross-repo-todo.md`. В этой сессии я потратила время, заново выясняя то,
что там уже было описано.

---

### Лично от владелицы

- [ ] 🔴 Переименовать `.claude/settings.json.example` → `.claude/settings.json`
- [ ] 🔴 Раздел A чеклиста кабинета — 9 правок (классификация, маркировка)
- [ ] 🟠 Заливка 39 описаний из `docs/seo/descriptions/`
- [ ] 🟠 Разделы B и C чеклиста — 25 правок
- [ ] 🔴 Расходы: `marketing_expenses`, `cash_flow`, `manual_expenses` пусты. Маржа завышена
- [ ] 🟠 Выгрузка воронки за год из кабинета WB
- [ ] 🟢 ~~Проверка утёкших паролей~~ — **проверено 22.08: недоступна.** Функция плана Pro,
      проект на Free. Лежала бы в Authentication → Sign In / Providers → Email
      (не в Policies, как было записано ошибочно). Ради одного тумблера апгрейд
      не нужен: в проекте один пользователь, посторонние не регистрируются, а защита
      от утёкших паролей нужна там, где приносят чужой скомпрометированный пароль.
      Бесплатная замена в том же блоке: минимальная длина 12 и требование цифр,
      строчных, заглавных и символов.
- [ ] 🟢 Удалить мусорный проект `arols` в панели Vercel — завёлся, когда `vercel deploy`
      случайно запустился из домашней папки

### Совместно

- [ ] Себестоимость — файл нужен только если про разбивку по партиям. Простая уже есть у всех 80 SKU
- [ ] Воронка за год — вы выгружаете, я разбираю и заливаю
- [ ] Разбор таблетниц — 5 карточек группы с просевшей конверсией
- [ ] Семантическое ядро — решить источник: WB Jam / Evirma / разбор 6996 отзывов
- [ ] Балл карточки — после ядра

### Могу сам

- [ ] ⚠️ **РАСХОЖДЕНИЕ РЕПО ↔ ПРОД: `telegram-alerts`.** В проде v23, в репо на один
      коммит больше — `margin_pct` умножался на 100 (список «топ-3 SKU где маржа упала»
      показал бы 500 п.п. вместо 5). Ветка редкая: срабатывает только при падении общей
      маржи больше чем на 15 п.п., поэтому не горит. **Задеплоить при следующей правке
      этой функции** — отдельный деплой ради этого не стоит риска: файл 35 КБ,
      передаётся вручную, опечатка ломает ежедневную сводку.

- [x] ✅ **RLS закрыт 22.08** (PR #232). Восемь таблиц в `public` были открыты анониму
      через PostgREST, две view исполнялись с правами владельца и обходили RLS.
      Включён RLS без политик: анонимный ключ в приложении используется только
      для аутентификации, данные читаются service-role клиентом server-side.
      Проверено: под `anon` все восемь таблиц дают 0 строк, под `service_role`
      данные на месте. ERROR-уровень линтера Supabase пуст — письма прекратятся.
- [x] ~~🔴 Оборачиваемость — 404 с 11.06~~ — **снято 22.08, задача была устаревшей.**
      В репо `fetch-wb-turnover` давно заглушка на 410 с пометкой DEPRECATED:
      WB удалил эндпоинт, оборачиваемость считается из view `v_turnover_by_sku`.
      Крона нет намеренно, чинить нечего.
- [ ] 🟠 `X-Cron-Secret` нет у 10 функций, включая продажи и заказы
- [ ] 🟠 `sync-sheets` падает на Google с 13.06
- [ ] 🟢 Регулярные снимки контента — сейчас разовый
- [ ] 🟢 `config.toml` не знает семи функций
- [ ] 🟢 Два `WARN` линтера Supabase, оставлены осознанно: `pg_net` живёт в схеме
      `public` (перенос рискует порвать cron-задания, которые через него дёргают
      Edge Functions), и выключена проверка утёкших паролей — это тумблер
      в дашборде, Authentication → Policies, только руками владелицы.

### Отложено осознанно

Поставки (405), реклама (крон отключён), автоматизация заливки карточек.

---

## [устарело] 2026-08-21 — Ревизия системы: что собирается, что сломано, что делать

Составлено по факту, запросами к проекту `hcebwgjgppwaguqittpi` и коду в репозитории.
Не по документации: она разошлась с реальностью, см. блок E.

**Главный вывод.** Механика построена и работает, входные данные неполные.
P&L считается по тому, что есть, и молчит о том, чего нет. Пока не закрыты
блоки A и B, ни маржа, ни выкуп не отражают реальность — а именно ими
предполагается мерить эффект от SEO.

---

### A. Данные не собираются — чинить первым

Всё это `v_data_quality` показывает прямо сейчас. Инструмент работает, в него не смотрели.
Детальный разбор расхождений репо ↔ прод — `docs/AUDIT_2026-08-21_repo-vs-prod.md`.

- 🔴 **Возвраты — ноль строк за всё время.** `fetch-wb-goods-returns` падает ежедневно:
  пишет в `wb_goods_returns_events`, такой таблицы в схеме нет. Есть `wb_goods_returns`, пустая.
  Проверить миграцию `20260613_wb_goods_returns_events.sql` — применена ли, и совпадает ли
  имя таблицы с кодом функции. **Без возвратов процент выкупа неверен.**
- ⏸ **Поставки — отложено 21.08 по решению владелицы, неактуально сейчас.**
  Сделано: v2 задеплоена, логирование переведено на `ingestion_log`.
  Осталось: WB отвечает `405 Method Not Allowed` на `GET /api/v1/supplies` (`ag-supplies`) —
  путь есть, метод не тот. Нужен разбор актуального контракта supplies-api, деплоем
  не лечится. Cron `fetch-wb-supplies-6h` и `fetch-wb-supplies-daily` отключены.
- 🔴 **Оборачиваемость не собирается с 11 июня.** `fetch-wb-turnover` — 404 от `s2sauth-ca`.
  Два с половиной месяца. Проверить актуальный эндпоинт WB.
- ✅ **Недостающие функции развёрнуты 22.08.** В проде было 20 функций из 25.
  Развёрнуты три, которые вызывались активными кронами впустую:
  - `fetch-wb-prices` (v1) — **86 цен**, 513–2938 ₽, средняя скидка 29.2%, битых нет.
    `wb_prices_fact` была пустой, теперь наполняется. Это входные данные для маржи.
  - `fetch-wb-feedback` (v1) — **6996 отзывов** с 16.10.2023 по 21.08.2026, 106 SKU,
    средний рейтинг 4.81. `wb_reviews_fact` была пустой.
  - `telegram-indices-reminder` (v1) — вручную не запускала, шлёт реальное сообщение;
    отработает по крону в понедельник.

  У всех трёх попутно починено логирование: писали в `integration_jobs`, которой
  в схеме нет — та же болезнь, что была у `fetch-wb-supplies`. Запись падала молча,
  и задачи не видели ни `v_data_quality`, ни `telegram-alerts`.

- ⛔ **`set-wb-price` и `create-wb-supply` намеренно НЕ развёрнуты.** Обе пишут в WB.
  Правило владелицы: работа только в одну сторону, в WB не пишем. Пока правило
  не изменится — эти две не деплоить. В них же остались последние обращения
  к несуществующей `integration_jobs`; чинить смысла нет, пока функции не нужны.

- 🟠 **Отдельно всплыло: 5056 отзывов без ответа** (1940 из 6996 отвечены).
  Не техническая задача, операционная — но теперь видна в базе.
- 🟠 **`sync-sheets` падает** на Google: «You can't remove all the sheets in a document».
- ✅ **Возвраты — почини 21.08.** Досоздана `wb_goods_returns_events`, маппинг приведён
  к текущему формату WB (`returnType` вместо `reason`, `completedDt` вместо `returnDate`),
  функция задеплоена и отработала со статусом `ok`.
- ✅ **Зомби-задачи закрыты 22.08.** Было 1504 записи в статусе `running`, из них 1481
  у `fetch-wb-ads` (копились с 20.06). Все закрыты.
  Заведена `clean_stale_running_jobs_all(interval)` — проход по всем задачам сразу,
  и крон `clean-stale-jobs-hourly` (`17 * * * *`, порог 2 часа). Больше не накопятся.
  **Почему не подключили существующий `try_job_lock`:** он берёт `pg_advisory_lock`,
  а advisory-блокировка живёт только внутри сессии Postgres. Edge Function ходит
  через PostgREST — сессия закрывается сразу после вызова, блокировка снимается сама.
  Подключение его к 24 функциям ничего бы не дало. Вероятно, поэтому и не подключили.

### B. Дыры во входных данных — без них ничего не сойдётся

Это не код, это ввод. Решение за владелицей.

- 🔴 **Расходы отсутствуют полностью.** `marketing_expenses`, `cash_flow`, `manual_expenses` —
  по нулю строк. Всё, что вне WB API: блогеры, упаковка, зарплаты, любые движения денег.
- 🔴 **Себестоимость у половины каталога.** 40 SKU из 80 без записи в `cogs_calculations`.
- 🟠 Следствие: `v_data_quality` показывает 2 SKU с отрицательной маржой
  (`AAND1RP301YL` при выручке 18 375 ₽, `AKTA1PR101GR` при 6 020 ₽) и 14 с маржой ниже
  целевой. **Отличить реальный убыток от артефакта неполных данных сейчас невозможно.**

### C. Мониторинг — почему поломки жили месяцами

- ✅ **`telegram-alerts` починен 22.08 (v23).**
  Нашлась настоящая причина: функция читала **последние 500 строк** `ingestion_log`.
  Продажи и заказы идут каждые 30 минут — 500 строк покрывают меньше двух суток,
  поэтому недельная `fetch-wb-commissions` в окно не попадала и объявлялась мёртвой.
  Отсюда противоречие в одном сообщении: «Комиссии обновлены 5 дней назад 🟢»
  и «нет успешных запусков 🟠» про одну и ту же задачу.
  Сделано: view `v_job_health` (агрегат по всей таблице, усечения нет), проверка
  переписана на него, добавлено условие «последний запуск упал» — раньше задачи,
  которые падали ежедневно, проходили молча (поставки: 32 ошибки за неделю, тишина).
  Мониторим только задачи с включённым кроном: выключенные намеренно не шумят.
  Проверено запуском: 200, сообщение доставлено.
  **Попутно доехал PR #196** «убрать обманчивые %» — смержен, но с 11.07 не деплоился.
  Поэтому дельта маржи показывалась как «Δ -8.1%», хотя это процентные пункты.
  Теперь «было X% → стало Y%».

- ⏸ **Прошлая формулировка (для истории):**
  Уточнение по скриншоту от владелицы: бот **работает** и ловит многое — провисший cron,
  критичные аномалии, OOS, падение маржи. Но 21.08 он показал только `fetch-wb-commissions`
  и пропустил поставки с возвратами, которые падали ежедневно. Похоже, проверка ищет задачи
  вообще без успешных запусков, а те, что пишут `error` в `ingestion_log`, мимо неё проходят.
  Чинить одно условие, а не весь мониторинг.
- 🟠 **RLS отключён у семи таблиц:** `manual_expenses`, `wb_prices_fact`, `wb_reviews_fact`,
  `wb_personal_indices`, `wb_supplies_v2`, `wb_supply_items_v2`, `delivery_to_wb_invoices`.
  Открыты на чтение и запись по anon-ключу. Сейчас пустые, но `wb_prices_fact`
  и `manual_expenses` наполнятся. Включать только вместе с политиками под `service_role`.
- 🔴 **Зомби-задачи: механизм есть, не подключён.** В базе с 20.06 живут `try_job_lock`,
  `release_job_lock`, `clean_stale_running_jobs` (миграция `job_advisory_locks`).
  **Ни одна из 24 функций их не вызывает** — отсюда 167 записей `running` у `fetch-wb-ads`.
  `AUDIT_PROMPT.md` эту проверку содержит с самого начала.
- 🟠 **`X-Cron-Secret` guard отсутствует у 10 функций**, включая `fetch-wb-sales`
  и `fetch-wb-orders`, которые крутятся каждые 30 минут.
- 🟠 **`config.toml` не знает семи функций** из репозитория, две из них при этом задеплоены.
- 🟠 **Деплой Edge Functions.** С 11.07 доехал один раз из восьми. Actions отключены,
  ручной путь есть (MCP `deploy_edge_function`) — зафиксировать его как рабочий
  и описать в `CLAUDE.md`, иначе следующая сессия снова решит, что деплоя нет.

### D. Карточки и SEO — 🔥 приоритет с 21.08

**Решение владелицы 21.08:** продаж мало, есть время заняться карточками вплотную.
Поставки и алерт отложены, блок D становится первым.

#### Задачи

Описания готовы и лежат в `docs/seo/descriptions/`, смержены в `main` (PR #219).

- 🔴 **Раздел A чеклиста кабинета** — 9 правок про классификацию и маркировку.
  Первым `AAND1ST202BL`: «зубная щётка» в трёх полях, позиция 9603 21, маркировка
  с 01.10.2026. См. `docs/seo/checklist-kabinet.md`.
- 🟠 **Заливка 39 описаний** в кабинет по `docs/seo/README.md`.
- 🟠 **Разделы B и C чеклиста** — пустые характеристики (капы: одно поле на восемь карточек)
  и 15 ошибок в данных.
- 🟠 **Частотность запросов.** `fetch-wb-search-queries` + таблица `wb_search_queries`
  по образцу `fetch-wb-funnel`. Фаза 1 — пробник, кладёт сырой ответ, чтобы увидеть
  реальный контракт API. Ядра в 14 файлах собраны экспертно и требуют сверки.
- 🟡 **Связь воронки с карточкой — сделана наполовину 22.08.**
  Сделано: view `v_sku_seo_funnel_30d` (просмотры, корзина, заказы, конверсии в разрезе SKU),
  колонка «Трафик 30д» и чип «Есть трафик и замечания» во вкладке `/seo`, сортировка
  по просмотрам внутри высокого риска. Теперь видно, где правка описания вообще
  на что-то повлияет: 71 SKU в воронке, 5756 просмотров за 30 дней.
  - 🔴 **По дороге найден дефект сбора, часть данных потеряна навсегда.**
    `fetch-wb-funnel` читал поле `addToCartCount`. До 13.06 оно работало, потом WB
    переименовал его в `cartCount` — и корзина лежала нулём **с 14.06 по 14.08**,
    61 день с живыми просмотрами. Исправлено и задеплоено (v32), заодно забираем
    `addToWishlistCount`, `addToCartConversion`, `cartToOrderConversion`,
    `buyoutPercent`; функция сама режет период окнами по 7 дней.
    **Перезабрать эти 61 день нельзя:** WB отвечает `400 invalid start day` на любую
    дату глубже примерно недели. Данные достоверны с 15.08. Поэтому конверсия
    в корзину во вкладке считается за 7 дней, просмотры и заказы — за 30.
    Сравнивать конверсию «до/после» правки описания можно только с 15.08.
  - ✅ **Точка отсчёта снята 22.08, до заливки.** Заведена `sku_content_snapshots`
    (название, описание, характеристики, дата, причина). Первый снимок —
    `reason = 'before_seo_upload'`: **80 карточек, все с описанием и характеристиками,
    169 337 знаков, 13 предметов WB.** Длина описаний сейчас: минимум 464, среднее 2117,
    максимум 4973 — то есть в среднем выше целевых 1500–2000.
    Старая `sku_snapshots` не годилась: она снимает витрину (цена, рейтинг, отзывы),
    полей `description` и `characteristics` нет ни в колонках, ни в `raw` — проверено.
    Плюс мертва с 05.06. Расширять её не стали, завели отдельную под контент.
  - 🟠 **Осталось: регулярный сбор снимков.** Сейчас снимок разовый, крона нет.
    Нужен после заливки — чтобы ловить и последующие правки, а не только эту.
    Решать спокойно, срочности больше нет: точка «до» зафиксирована.
- ✅ **Вкладка «SEO карточек» — сделана 22.08 (PR #221).** Сводка по всем 80 SKU из
  `v_sku_seo_issues`: чипы риска, фильтр по предмету WB, поиск, топ повторяющихся находок
  с фильтрацией по клику, разбор карточки, выгрузка CSV. Плюс блок в `product-detail`
  и ссылка `/seo?article=` из него. Собрано на общих компонентах (`DataTable`, `SkuThumb`,
  `ExportCsvButton`) — как каталог, а не отдельным островом.
  **Балл сознательно не считаем**: без семантического ядра он мерил бы дисциплину,
  а не результат. Вернуться к нему после частотности.

### E. Документация — ✅ приведена в соответствие 21.08

`PLAN.md` переписан по факту, `README.md` — стек и точки входа, `CLAUDE.md` — среда,
деплой и запуск функций, `CRONS.md` — 19 задач и исправленная команда,
`AUDIT_PROMPT.md` — контекст и новые проверки.

Осталось: `tasks/SESSION_LOG.md` — пишется по триггеру конца сессии.

<details>
<summary>Что было не так (для истории)</summary>

### Было: документация разошлась с системой

Каждый пункт — то, на что ИИ опирается при принятии решений.

- 🔴 **`CLAUDE.md`** — «Edge Functions деплоятся CI» при отключённом CI; не сказано,
  что функции запускаются из SQL через `net.http_post` + Vault. Обе строки уже привели
  к неверным выводам в сессии 21.08.
- 🔴 **`docs/PLAN.md`** — Phase 2 «ждёт токен» (работают 19 функций), Phase 3 «10 view,
  данных нет» (36 view, данные идут), Phase 5 «Lovable + Google Sheets» (Next.js 15,
  26 вкладок), Phase 7 «Telegram потом» (работает).
- 🔴 **`README.md`** — «Frontend: Lovable / React». По факту Next.js 15 + React 19,
  feature-sliced. Нет строки про контур карточек.
- 🟠 **`docs/CRONS.md`** — 12 задач из 19. Нет `fetch-wb-feedback-daily`,
  `fetch-wb-prices-daily`, `fetch-wb-supplies-6h`, `detect-anomalies-hourly`,
  `fetch-wb-ads-hourly`, `telegram-indices-reminder-monday`,
  `refresh-sku-weekly-metrics-daily`. Команда отключения написана с `jobid`,
  правильно `job_id` — команда из документации не выполняется.
  Плюс зафиксировать, что три задачи отключены 21.08 и почему.
- 🟠 **`tasks/SESSION_LOG.md`** — верхняя запись 18.08, после были сессии 20.08 и 21.08.

</details>

---

### Предлагаемый порядок

1. **A + C** — вернуть сбор данных и починить мониторинг. Иначе следующая поломка
   снова проживёт месяц незамеченной.
2. **B** — заполнить расходы и себестоимость. Без этого P&L декоративен.
3. **E** — документация. Дешёво, а экономит каждую следующую сессию.
4. **D** — SEO. Заливка не блокируется ничем и может идти параллельно;
   частотность и вкладка — после того, как данные начнут сходиться.

Спорный пункт: D можно поднять выше, если карточки горят по срокам маркировки
(01.10.2026 по коду 9603 21). Раздел A чеклиста стоит сделать в любом случае первым.

---

## [устарело] 2026-07-11 — Ожидаем список визуальных багов от владелицы

Всё крупное из плана закрыто (см. `tasks/SESSION_LOG.md`). Владелица тестировала UI — соберёт список визуальных багов на следующую сессию.

- 🔴 **Список визуальных багов** от владелицы — прилетит в следующей сессии, обрабатываем по одному
- 🔴 **Фото Топ-5 / Категории** — проверить визуально что грузятся через `tm/1.webp`
- 🔴 **Левый/правый блоки одной высоты** — визуальная проверка (`[&>*]:h-full`)
- 🔴 **WorktreeCreate hook** — проверить на параллельных subagent'ах

---

## [устарело] 2026-06-26 — Сейчас в работе

- 🟢 **PR #175 — 4 карточки утреннего брифа (заказы + выкупы)** — задеплоено, ждёт визуальной проверки.

## [устарело] Очередь UX-фиксов (по одному, после визуальной проверки каждого)

> Правило: один баг → фикс → визуальная проверка → следующий. Не скопом.
> Легенда: 🟢 закрыто (оставляем как референс) · 🔴 не сделано · 🟡 в работе.

1. 🟢 **Средняя маржа 58.3% (Продажи за 30 дней)** — weighted avg по revenue (PR #176 merged) + deeper fix в `get_full_pnl_by_period` (PR #177). _Скрин: 2026-06-25._
2. 🟢 **Расхождение выручки между разделами** (PR #177 — `retail_amount` + унифицированное `rr_dt`-окно):
   - /products «Мои товары»: **197 940 ₽** = raw факт (`get_catalog_sales_daily`) ✅
   - /sales-report «Отчёт по продажам»: **197 940 ₽** = raw факт (`get_sales_report_daily`) ✅
   - /dashboard «Сводка»: **295 742 ₽** (`get_full_pnl_by_period`) ❌ +50%
   - /pnl «Прибыль и убытки»: **295 742 ₽** (`get_full_pnl_by_period`) ❌ +50%

   **Корень бага в `get_pnl_by_period`:**
   1. `SUM(retail_price × quantity)` — цена ДО скидки × шт. Должно быть `SUM(retail_amount)` (готовая сумма после скидки).
   2. Revenue считается по `sale_dt`, расходы по `rr_dt` — разные даты, период не сходится.

   Фикс: переписать `get_pnl_by_period` чтобы revenue был `SUM(retail_amount) WHERE doc_type IN ('Продажа','Возврат') AND rr_dt BETWEEN p_from AND p_to`. Сверить через MCP execute_sql до/после (rules.md §16).

3. 🟢 **/pnl — KPI-цифры перед графиком** (PR #182) — 4 KPI карточки (Доходы / Расходы / Прибыль / Маржа%) с delta vs прошлый период.
4. 🟢 **/pnl — структура доходов** (PR #182) — IncomeBreakdown по subject_name, симметрично ExpenseBreakdown.
5. 🟢 **Критичные SKU — отдельная страница /products/critical** (PR #181) — группировка по причинам (out_of_stock / no_sales_14d / other), MorningBrief ссылка обновлена.
6. 🔴 **Матрица ABC × PPP — клик по квадратику** — `/analytics`: при клике на любой квадрат (например «PPP × A: 4 шт») переход на список товаров **именно из этой группы**. Сейчас не кликабельно. _Скрин: 2026-06-25._
7. 🔴 **Матрица XYZ оборачиваемости — клик по карточке** — то же что ABC×PPP: клик на «X: 0 шт», «Y: 0 шт», «Z: 79 шт» → список SKU из этой группы. _Скрин: 2026-06-25._
8. 🟢 **Русифицирован UI на /products/costs** (PR #180) — заменены cost / CSV-headers / UNIT / XLSX на русские.
9. 🔴 **Себестоимость: ручной ввод полей вместо только Excel** — в `/products/costs` дать возможность редактировать данные inline в ячейках таблицы, не только через файлы. _Скрин: 2026-06-25._
10. 🔴 **/analytics/weekly — нет цифр** — раздел «Аналитика по неделям» (год 2026): Единиц продано 0, Выручка 0 ₽, Прибыль 0 ₽, Маржа 0.0%, график «Нет данных», таблица «Нет данных за 2026». Источник `sku_weekly_metrics` (импорт из `UNIT_WB по неделям 2026.xlsx`). Проверить: (а) есть ли данные в таблице, (б) запрос фильтра по году, (в) маппинг колонок при импорте. _Скрин: 2026-06-25._
11. 🟢 **Маржа 56% → 30% → 2.3%** (PR #179) — два шага в get_pnl_by_period: убран фильтр doc_type из exp CTE + запрос идёт от UNION(rev.nm_id, exp.nm_id) вместо sku_catalog. Остался не учтён marketing — задача 11a.
12. 🟢 **Средний чек 107 → 442 ₽** (PR #183) — orders = COUNT(DISTINCT srid) FILTER (doc_type_name=Продажа, quantity>0). Раньше считались все строки финотчёта.
13. 🟢 **«Отмены» → «Возвраты» в UI** (PR #183, частично) — поле cancellations в RPC = возвраты. Реальные отмены 13.1% — задача 13a (новая RPC по wb_orders_fact.date).
14. 🔴 **/promo (Промо-акции WB) — все колонки пустые** — матрица «80 товаров × 6 акций»: «Цена» —, «Маржа сейчас» —, и во всех колонках акций (Excel TEST Акция 1-4, Спортивные скидки, Жаркие скидки-2) у всех 80 SKU стоит «—». «Оборачив.» отрисована (например 1491 д) — значит данные товаров грузятся, но цена/маржа/участие в акциях не подтягиваются. Проверить: (а) RPC/запрос за `current_price` и `wb_promotions_fact`, (б) маппинг promotion_id → SKU. _Скрин: 2026-06-25._
18. 🔴 **/expenses (Мои расходы) — реализовать ручной ввод** — placeholder «Появится вместе с M2 (P&L)». Нужно: таблица manual_expenses + форма UI + подмешать в get_full_pnl_by_period отдельной строкой «Прочие расходы». _Скрин: 2026-06-25._
17. 🔴 **/tariffs (Тарифы и коэффициенты) — обновлять автоматически по понедельникам** — сейчас базовые тарифы логистики WB (Common Tariffs API) и индексы локализации/распределения «Обновлено 01.06» — старее месяца. Настроить cron: каждый понедельник в 06:00 MSK дёргать `fetch-wb-tariffs` (Common Tariffs API), записывать в `wb_tariffs_fact` с `effective_from = понедельник_недели`. Индексы локализации/распределения — оставить ручной ввод раз в неделю (как в подписи). _Скрин: 2026-06-25._
16. 🟢 **/reviews — бейдж «Демо-данные»** (PR #185, частично) — пометка что страница на mock-фикстуре. Полная интеграция WB Feedback API — задача 16a.
15. 🟢 **/deficit — только реально требующие поставки** (PR #184) — totalRows = outOfStock + critical + warning + filterRealDeficit() оставляет daysLeft≤14 или stock=0. Раньше показывало все 66 SKU.

### 🟡 Подзадачи (появились в работе)

- **11a — marketing в марже**: подключить `wb_ads_fact` (через `get_real_marketing_for_period`) в `get_full_pnl_by_period`. Сейчас `marketing_expenses` пустая, реклама не учитывается → маржа 2.3% возможно ещё чуть ниже реальности.
- **13a — реальные отмены**: в `wb_orders_fact` за 30д **13.1%** отмен. Нужна отдельная RPC по полю `date` (не `rr_dt`) + KPI карточка на /sales-report или /dashboard.
- **14a — fetch-wb-prices**: новая edge function + таблица `wb_prices_fact` + cron. Без неё /promo не покажет цены/маржу для не-участников акций.
- **16a — fetch-wb-feedback**: новая edge function + таблица `wb_feedback_fact` + RPC агрегаций → заменить mock на /reviews.

## 🔮 Перспектива (по концепции «всё в проге, никаких импортов»)

> Принцип: константы и заказы вводятся **в проге при каждой поставке**, прога считает поставки и **генерирует** ТЗ-ФФ. Долгосрочно WB-токен с правом записи → автопуш поставок в WB + ФФ.

- [ ] **История констант** — при каждом вводе курса юаня/доллара/доставки/себеса записывать снапшот (`tariff_constants_history`). На /dashboard «Сводка» отдельный график: курсы (CNY/USD/доставка) + себес-перевешенная средняя + продажи на одном тайм-лайн — видно как изменения констант влияют на маржу.
- [ ] **COGS по FIFO из sku_cost_history** — каждая запись `cost` с датой = отдельная «партия» товара. При продаже списываем по методу FIFO (первый пришёл первый ушёл, как в 1С у владелицы). Даст 1:1 совпадение с её книгой «PL WB». Сейчас `cogs = cost_price × units` (current), даёт расхождение ~10% с её файлом.
- [ ] **Калькулятор поставки** — UI на основе `v_supply_recommendation`: прога считает qty per склад по продажам / остаткам / lead_time / safety_stock. Кнопка «Создать поставку» → попадает в `wb_supplies_planned`.
- [ ] **Генератор ТЗ-ФФ** — экспорт Excel (или CSV) из калькулятора поставки → файл скачивается → отправляешь ФФ. Заменяет ручную работу с шаблоном.
- [ ] **WB Write API integration** — `/api/v1/supplies` POST для создания поставок на WB прямо из проги (нужен WB-токен с write-правом).
- [ ] **Форма «Новый заказ Китай»** — UI ввода **строго по структуре файла владелицы** `Order_china_*.xlsx` (поля и порядок 1:1 как в Excel). Заменит импорт. Парсер `china-order` уже знает структуру — переиспользовать те же поля в форме.

## ⏸ Большие задачи на ожидании

- [ ] **PR-B v2 fetch-wb-supplies** — переделать под FBW Supplies API (POST `/api/v1/supplies` + GET `/api/v1/supplies/{ID}/goods` на host `supplies-api.wildberries.ru`). Прежняя версия на `/api/v1/supplier/incomes` вернула WB 404. Свежий swagger — `https://dev.wildberries.ru/swagger/orders-fbw` (требует VPN).
- [ ] **PR-C delivery_to_wb_invoices** — после PR-B v2: таблица счетов от ФФ за доставку поставки + UI «Поставки» + автоматический расчёт `delivery_to_wb_rub_per_unit`.
- [ ] **🚨 WorktreeCreate hook** — сделан в этой сессии (`AI_OS/.claude/hooks/worktree-*`), требует проверки на параллельных subagent'ах.
- [ ] **Фото в Топ-5 / Категории** — проверить визуально после деплоя что подгружаются через `tm/1.webp`
- [ ] **Левый/правый блоки одной высоты** — реализовано через `[&>*]:h-full`, проверить визуально

---

## 🟢 Готовы к merge (16 PR от 20-21.06)

Все зелёные на CI (Vercel preview success, типы и билд проходят). 7 PR с включённым `enable-automerge` сольются автоматически, остальные нужно мерджить вручную или ре-триггерить CI.

### С enable-automerge — сольются сами:
- **#159** test(vitest): unit-тесты финансовых формул (50 кейсов в 7 файлах)
- **#158** refactor(fetch-wb-orders): paginateByLastChangeDate с onPage-callback
- **#156** docs(todo,rules): закрыть выполненное + §16 правило сверки RPC

### БЕЗ enable-automerge — нужен ручной merge или re-trigger CI:
- **#157** refactor(wb-client): paginateByLastChangeDate финиш + миграция fetch-wb-sales
- **#155** refactor(rpc): xlsx-export через RPC (9/9 range)
- **#154** refactor(rpc): sales-report (8/9)
- **#153** refactor(rpc): analytics (7/9)
- **#152** refactor(rpc): catalog (6/9)
- **#151** refactor(rpc): business-snapshot ×3 функции (5/9)
- **#150** refactor(rpc): price-simulator (4/9)
- **#149** refactor(rpc): sources (3/9)
- **#148** refactor(rpc): supplies (2/9)
- **#147** refactor(rpc): data-quality channelGaps (1/9)
- **#146** security(cron): X-Cron-Secret guard в 10 edge functions
- **#145** refactor(wb): унификация fetch-wb-{sales,orders,ads} через _shared/wb-client.ts

### Чужая ветка (PR #144 на `claude/funny-cerf-s37pkh`):
- **#144** feat(autosebes): 3 доп тарифа + extend v_sku_cost_breakdown + FF Excel — это автосебес-задача с предыдущей сессии, не из этой.

---

## ⏸ Требует решения / приостановлено владелицей

- **🔒 Макс. безопасность (3 пункта)** — решение владелицы 21.06: **«всё оставляем как есть, ничего не меняем по входу»**. План полностью зафиксирован в `docs/SECURITY_PLAN.md` (verify_jwt + service_role JWT в pg_cron + Supabase Auth magic-link + 15-минутная Dashboard-сессия). **Триггерные фразы для возобновления: «безопасность», «план по безопасности», «друзьям предложить», «Dashboard сессия».** Текущая защита: `X-Cron-Secret` (PR #146 после мерджа) + Origin/Referer гигиена (PR #138).
- **Multi-tenant fork** (если когда-нибудь подключать друзей) — решение 21.06: **«делаем пока только для меня»**. План в `docs/SECURITY_PLAN.md` (3 варианта + 5 вопросов) + детальный чек-лист в `docs/MULTI_TENANT_PLAN.md`. Возобновить когда «созреет».

---

## 🟡 Можно делать в фоне без согласования (все ✅ закрыты в этой сессии)

- ✅ **`.range(0, 200_000) × 9 файлов` → RPC агрегация** — 9 PR (#147-155). Каждый со сверкой старое=новое через MCP execute_sql.
- ✅ **CRON_SHARED_SECRET helper для 10 edge functions** — PR #146. После мерджа — 2 команды от владелицы (см. `docs/SECURITY_PLAN.md`).
- ✅ **wb-client.ts helpers + paginateByLastChangeDate** — PR #145, #157, #158.
- ✅ **Vitest + 50 кейсов финансовых формул** — PR #159 (classifyProfit/Sales/Stability, buildRecommendation, computeBreakEven, business-rules snapshots).
- ✅ **WorktreeCreate hook** — задача зафиксирована (требует ручной настройки `~/.claude/settings.json`).

---

## 🔮 Backlog (новые Edge Functions / большие фичи)

- **Автоматический себес** roadmap — в работе на ветке `claude/funny-cerf-s37pkh`, PR #144.
- **TVV (Видимость/Доверие/Ценность)** — ⏸ отложили
- **Goals по SKU** — ⏸ отложили
- **Office Add-in / Power Query** — ⏸ отложено
- **Импорт 22 старых бланков заказов Китай** — ⏸ отложено (после стабилизации БД)

---

## ⏸ Ждём от пользователя

- Excel «Фулфилмент», «Поставки» — для автосебеса (Excel Заказов Китай уже импортируется через `/products/costs`)
- **После мерджа PR #146** — установить `CRON_SHARED_SECRET` в Supabase Secrets (см. `docs/CRONS.md` + `docs/SECURITY_PLAN.md`)

---

## 🔵 Перспектива (записано, не делать без явного запроса)

- Google Sheets sync — на паузе по решению владелицы
- Полный multi-tenant (sign-up + billing + org-isolation) — см. `docs/MULTI_TENANT_PLAN.md`
