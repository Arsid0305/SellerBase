# Tasks — SellerBase

> Живой TODO. Принцип: один список, один порядок приоритета. Устаревшие секции удаляем, не накапливаем.

---
## [актуально] 2026-09-04 — Аудит репо: что осталось после чистки

### Ждёт владелицу

1. **Залить 214 правок из книги** — карточки, главное по приоритету.
2. **Деплой `fetch-wb-promotions`** — отклонён 04.09. Новая версия в ветке,
   в проде старая. Пока не задеплоена, метрики участия в акциях не собираются.
3. **Включить cron акций** — вместе с деплоем, не раньше.
4. **`yc-functions/`** — объяснение не дано, решение не принято.
5. **Мерж PR по этой ветке и PR #297.**

### Моя работа, не начата

**`fetch-wb-supplies` — чинить** (решение владелицы 04.09). WB отдаёт 405
на `ag-supplies`, 160 ошибок, оба задания отключены. Способ: разведать
актуальные пути `supplies-api` через `probe-wb-api` — с акциями это сработало.

**`fetch-wb-ads`** — 758 ошибок «зависла в running», задание отключено.
Решения нет: чинить, удалить или оставить выключенным.

**Тестовый мусор в проде:** `wb_promotions` содержит четыре записи
`Excel TEST: Акция 1–4` (`type='test_excel'`, 12.06). Попадают в матрицу `/promo`.
Спросить перед удалением.

### Сделано 04.09

Правило §26 «одна точка входа» и приведение карт разделов; правда в README
(`sql/views`, счётчик функций, битая ссылка, байткод в git); `CRONS.md`
пересобран из `cron.job` — было 13 заданий из 21; починен упавший
`build_group_artifact.py`; шесть книг Excel вынесены, `tasks/` с 6,2 МБ до 964 КБ.

---

## [устарело] 2026-09-03 (вечер) — Девять групп разобраны, этап карточек закрыт

**Владелица 03.09: «На данном этапе останавливаюсь. Карточки! Потом подчищать
будем».** Разбор карточек завершён, всё остальное отложено.

### Состояние книги

67 карточек, **214 правок и 24 вопроса**. Девять групп разобраны полностью,
включая две подгруппы, которых пересборка раньше не касалась.

| лист | карточек | правок | вопросов |
|---|---|---|---|
| Мешки для стирки | 12 | 64 | — |
| Массажёры | 23 | 61 | 8 |
| Игрушки для животных | 6 | 31 | 10 |
| Таблетницы | 12 | 16 | — |
| Ножи для пиццы | 2 | 11 | — |
| Эспандеры | 3 | 11 | 6 |
| Пробки для бутылок | 2 | 7 | — |
| Скакалки | 3 | 6 | — |
| Шпатели кондитерские | 1 | 4 | — |
| Бандажи косметические | 3 | 3 | — |

### Ждёт владелицу — по порядку

1. **Залить 214 правок.** Готовы и проверены, лежат без движения.
2. **Таблица весов юнит-экономики.** В характеристиках таблетниц 160 г
   у всех десяти против 165 / 170 / 150 в описаниях; веса идут из этой
   таблицы прямо в книгу.
3. **24 жёлтых вопроса:** цвет у семи массажных мячей (`106`, `107`, `109`),
   замер мячей для собак (ширина и глубина при диаметре 7 см),
   нагрузка `ACRB5FR200CL` — 30 или 45 кг, когда придёт партия.
4. **Страна «Того»** у `ACRB1MS106BС` — поправить на Китай в кабинете.
5. **Габариты семи одиночных мешков** — упаковки или предмета.
6. **Мерж PR #297.**

### [отложено] Подчистить потом

Всё ниже — не про карточки, поэтому вынесено из работы по решению владелицы.

**Консервные ножи выпали из книги.** `AKTA1KN101GR` даёт 12 заказов за год,
карточка активна, разбор написан 21.08 — но выгрузки кабинета по группе нет,
и в книгу она не попадает. Нужна выгрузка «Консервные ножи».
То же у кап (8 карточек, 0 заказов — владелица: «не трогаем, пока
не продаются») и пресса для чеснока (отключён).

**Промопульт — развести ядра.** У пробок, бандажей и мешков несколько
карточек тянут один набор кластеров, хотя по Джему продают по разным словам.
Раскладка «какой карточке какие кластеры» не готовилась.

**Мусор в ядрах Промопульта.** У пробок висят чужие товары: «пробка для
бутылки масла» 1 815, «насадки для бутылки со вкусами» 7 310, «пробка
резиновая для колбы» 1 043, «пробки для бутылок новогодние» 1 275.

**Срезы Эвирмы по узким запросам, где мы сильны.** Сняты по общим запросам,
а заказы идут с узких: «скребок для теста» (позиция 18), «закрывашка
для вина» (6), «бандаж для челюсти» (12) — по ним срезов нет.

**Цены против рынка.** Ножи, шпатели, мешки и бандажи стоят выше медианы
топ-20 в 1,8–4 раза. Вопрос к юнит-экономике, не к карточке.

### [решает владелица] Находки аудита репо — 04.09

Проверено фактами, само не чинится: каждое требует её решения.

**Акции WB не обновляются с 12.06.2026.** Cron `fetch-wb-promotions-daily`
заведён миграцией `20260622000003`, но в `cron.job` его нет. `wb_promotions`
и `wb_promotion_items` — по 13 строк, последняя выгрузка 12.06. Матрица
`/promo` показывает данные трёхмесячной давности. Возврат — применить ту же
миграцию через MCP; отмена — `cron.unschedule`. **Вопрос не технический:
участвуем ли в акциях WB.**

**Три cron-задания отключены.** `fetch-wb-supplies-daily` и
`fetch-wb-supplies-6h` — **причина найдена 04.09: WB перевёл метод с GET
на POST.** `POST /api/v1/supplies` с телом `{limit, offset}` отдаёт 200
и данные; прежний `GET` — 405. Постранично теперь через `offset`, а не через
курсор `next`. Осталось переписать функцию и проверить второй её запрос,
`/api/v1/supplies/{id}/goods`.
`fetch-wb-ads-hourly` — 758 ошибок «зависла в running», последняя 21.08.
Решить по каждому: чинить, удалить или оставить выключенным.

**`yc-functions/` — мёртвый слой.** Yandex Cloud заводился, чтобы обойти
блокировку WB по IP (429 на `reportDetailByPeriod` с европейских адресов).
Сейчас cron туда не ходит ни одним заданием, а `fetch-wb-report` с Supabase
отрабатывает штатно — 7 успешных прогонов, последний 01.09, ошибок за
45 дней нет. Код оставлен как обходной путь на случай возврата блокировки,
в README каталога проставлена пометка. **Удалять или держать?**

**Шесть книг Excel в `tasks/excel-from-owner/`, 6 МБ** — половина веса
репозитория. Из них читается скриптами только
`UNIT_economics_cogs_tariffs.xlsx`. Остальные пять — исходники,
из которых система уже забрала данные. **Оставить, вынести из git
или удалить?**

**Четыре файла аудита** (`AUDIT_2026-06-17`, `06-20`, `08-21` и SEO-аудит) —
19 находок, 4 критических, статус ни одной нигде не отмечен. Отложено
отдельным пунктом ниже.

### Каноны, записанные в эту сессию

| § | о чём |
|---|---|
| §21 | в карточках только короткий дефис; комплектация с заглавной |
| §22 | подсветка сверяется с живым кабинетом, а не с ручной выгрузкой |
| §23 | три источника разбора: Джем, Эвирма, Промопульт — и чего не видит каждый |
| §24 | топовые ключи не отбрасываем, подстраиваем воронку |
| §25 | блок «Честно» упразднён, говорим преимуществами |

### Выведено из ассортимента — девять артикулов

`AAND1ST202BL`, `ACRB1SK201BC`, `AKTA2KN101BL`, `ACRB5FR400CL`,
`AKTA4ST102CL`, `AHMA4BW401BG`, `AHMA6BW101BG`, `ACRB1MS109WH`,
`ACRL1MS101RJ` — все в `docs/seo/discontinued.json` с датой и причиной,
чтобы не завелись заново при следующей выгрузке.

---

### [отложено] Вернуться к четырём аудитам — статус находок не отмечен нигде

Четыре файла `docs/AUDIT_*.md` содержат находки, по которым **не видно,
закрыты они или нет**: ни в журнале сессий, ни в правилах, ни в самих файлах
статуса нет. Ссылок на них почти нет — но это признак, что аудит написали
и забыли, а не что он не нужен.

| файл | крит | важных | nice |
|---|---|---|---|
| `AUDIT_2026-07-12.md` | **4** | 8 | 7 |
| `AUDIT_2026-06-17.md` | 1 | 1 | — |
| `AUDIT_2026-06-20.md` | 1 | 1 | — |
| `AUDIT_2026-08-21_repo-vs-prod.md` | — | — | — |

**Почему нельзя просто удалить.** Крит-находки июльского аудита — про деньги:

1. **F1** — две конкурирующие формулы `cogs+tax` в `get_pnl_by_period`
   и `get_daily_pnl_series`. Проверено 03.09: обе функции живы в проде
   и обе считают cogs. Совпадают ли формулы — не сверялось.
2. **F2** — новые edge functions без `pg_advisory_lock`, возможны зомби-джобы.
3. **F3** — `InvoicesPanel` вызывает side-effect в render.
4. **F4** — шесть edge functions не описаны в `supabase/config.toml`.

**Что сделать:** пройти находки по списку, каждую пометить «закрыта / актуальна»
с датой и доказательством (запрос к проду, ссылка на PR). После этого решить
по каждому файлу — оставить с отметками или удалить.

Тогда же вернуться к вопросу про `docs/AUDIT_2026-07-12.md`: удалять его
до проверки нельзя, это потеря информации о возможных дефектах в расчётах
P&L и себестоимости.


---

> **История задач — не здесь.** Закрытые сессии и решения лежат
> в `tasks/SESSION_LOG.md` (журнал append-only) и в истории git.
> Этот файл держит только актуальное: одиннадцать устаревших секций
> за 26.06–03.09 удалены 03.09.2026 по правилу из шапки.
