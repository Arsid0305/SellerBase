# Известный долг: объекты, которые есть в рабочей базе, но которых
# не создаёт ни одна миграция репозитория.
#
# Зачем файл. Проверка schema-drift сравнивает рабочую базу с той, что
# собирается из миграций. Если ронять проверку на давнем долге, она будет
# красной всегда. Красная всегда — значит её перестанут читать, а это ровно
# та болезнь, от которой проверка и заводится.
#
# Поэтому: проверка падает только на НОВОМ расхождении, сверх этого списка.
#
# История числа: 174 → 130 → 57 → 2.
#   • 174 — снято 12.09.2026, когда проверка завелась;
#   • 130 — описаны 11 объектов (миграция 20260912170000);
#   • 57 — удалены 7 пустых (миграция 20260912180000), 73 строки списка
#     стали лишними;
#   • 2 — описан весь остаток (миграция 20260912230000): таблицы problems,
#     notifications, notification_settings, notification_subscribers со
#     своими колонками, ключами и индексами, плюс три индекса
#     wb_reports_fact_oper_idx, wb_tariffs_box_date_idx,
#     wb_tariffs_box_wh_idx.
#
# Оставшиеся два — это индексы по колонке source у customer_personas и
# purchase_scenarios. Они и в файлах есть, и в рабочей базе, но названы
# по-разному: personas_source_idx против customer_personas_source_idx и
# scenarios_source_idx против purchase_scenarios_source_idx. Заводить
# вторые нельзя — в собранной из файлов базе вышло бы по два одинаковых
# индекса. Поэтому миграция 20260912230000 их переименовывает, и после
# её применения к рабочей базе эти две строки станут лишними, а список —
# пустым. Тогда рабочую базу впервые можно будет собрать из файлов
# один в один.
#
# Ранее в шапке значилось, что в долге остаются представления (включая
# недельный P&L) и функция get_sources_by_period. Это было неверно: оба
# создаются миграциями 20260611003004 и 20260621000002. Разобрано поимённо
# 12.09, запись исправлена.
#
# Формат: по одному отпечатку строки перечня в строке. Перечень строится
# запросом supabase/ci/schema-inventory.sql, отпечаток — left(md5(line),8).
0a580765
c659c8ab
