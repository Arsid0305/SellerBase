# Известный долг: пусто.
#
# 12.09.2026 рабочая база и база, собранная из файлов репозитория, впервые
# совпали один в один: 1178 объектов против 1178, расхождений нет.
# Проверено прогоном schema-drift на живой базе после применения
# миграции 20260912230000.
#
# Как шло: 174 → 130 → 57 → 2 → 0.
#   • 174 — снято 12.09, когда проверка завелась;
#   • 130 — описаны 11 объектов (20260912170000);
#   • 57  — удалены 7 пустых (20260912180000), 73 строки стали лишними;
#   • 2   — описан весь остаток (20260912230000);
#   • 0   — переименование индексов доехало до рабочей базы, последние две
#           строки стали лишними.
#
# Что это значит. Раньше рабочую базу нельзя было собрать из файлов: часть
# программы просто не встала бы. Теперь можно.
#
# Файл остаётся пустым намеренно. Если сверка снова что-то найдёт — это
# НОВОЕ расхождение, и лечить его надо миграцией, а не строкой здесь.
# Добавлять сюда отпечатки можно только сознательно и с объяснением,
# что именно за долг и почему он пока не закрыт.
#
# Формат: по одному отпечатку строки перечня в строке. Перечень строится
# запросом supabase/ci/schema-inventory.sql, отпечаток — left(md5(line),8).
