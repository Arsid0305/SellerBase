-- Закрыть утечку в представлениях, заведённых сегодня.
--
-- Найдено проверкой 17.09.2026. Таблицы закрыты RLS, и обращение к ним с
-- публичным ключом отдаёт пустоту - это правильно. Но представление в
-- Postgres по умолчанию читает данные правами того, кто его создал, а не
-- того, кто спрашивает. RLS при этом не применяется.
--
-- Проверено запросом из самой базы к своему же публичному адресу с ключом,
-- который лежит в браузере у любого посетителя сайта:
--   /rest/v1/v_stock_money  -> 200 и деньги владелицы
--   /rest/v1/wb_stocks      -> 200 и пустой список
--
-- То есть суммы остатков и прибыли мог прочитать кто угодно. Чинится
-- переключением представления на права спрашивающего: тогда RLS работает,
-- публичный ключ получает пустоту, а программа ходит служебным ключом и
-- видит всё как раньше. Проверено: эти представления читает только
-- серверная часть через служебный ключ.
--
-- Здесь закрыты только те три, что я завёл сегодня. Ещё девять таких же
-- представлений были в базе до этого - они вынесены владелице отдельно,
-- правки без её слова не делаю.

alter view public.v_stock_money            set (security_invoker = on);
alter view public.v_ozon_pnl_by_month      set (security_invoker = on);
alter view public.v_ozon_zalezhi           set (security_invoker = on);

-- Права на запись в представление бессмысленны, но выданы по умолчанию.
-- Убираем, чтобы не выглядело разрешением.
revoke insert, update, delete, truncate on public.v_stock_money       from anon, authenticated;
revoke insert, update, delete, truncate on public.v_ozon_pnl_by_month  from anon, authenticated;
revoke insert, update, delete, truncate on public.v_ozon_zalezhi       from anon, authenticated;
