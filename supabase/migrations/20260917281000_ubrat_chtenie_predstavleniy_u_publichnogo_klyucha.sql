-- Публичному ключу эти представления не нужны совсем.
--
-- Переключение на права спрашивающего (миграция выше) уже отдаёт нули
-- вместо денег. Но названия строк всё равно видны, и сам факт доступа
-- лишний: программа читает эти представления только серверной частью,
-- служебным ключом. Публичному ключу здесь делать нечего.
--
-- Служебный ключ (service_role) правами не ограничен, программа не ломается.
-- Роль authenticated оставляем: она понадобится, если раздел когда-нибудь
-- будет читаться из браузера под входом, и RLS там уже работает.
--
-- Проверено после применения: публичный ключ получает 401 «permission
-- denied for view v_stock_money», программа читает все 8 строк.

revoke select on public.v_stock_money       from anon;
revoke select on public.v_ozon_pnl_by_month from anon;
revoke select on public.v_ozon_zalezhi      from anon;
