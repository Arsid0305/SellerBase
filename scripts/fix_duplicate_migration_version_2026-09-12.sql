-- Развести два файла миграций с одним номером. Выполнено 12.09.2026.
--
-- Номер 20260620000001 носили сразу два файла: rpc_channel_gap_dates.sql
-- и v_sku_cost_breakdown.sql. В истории рабочей базы номер значился один
-- раз, и предохранитель — он сравнивает УНИКАЛЬНЫЕ номера — считал его
-- применённым. А CLI работает пофайлово: второй файл для него оставался
-- новым, и выкладка попыталась применить его заново. Упала на «cannot
-- drop columns from view», данные не тронуты.
--
-- v_sku_cost_breakdown.sql переименован в 20260620000002_*. Порядок сборки
-- не изменился (в имени _rpc_ идёт раньше _v_). Проверено пересборкой на
-- чистой базе: 146 миграций, 1123 объекта, ноль отличий.
--
-- Здесь записывается новый номер: представление в рабочей базе есть,
-- это фиксация факта. Данные не затронуты, запрос идемпотентен.
--
-- Предохранитель теперь ловит такие дубли сам и роняет проверку.

insert into supabase_migrations.schema_migrations (version, name)
values ('20260620000002','20260620000002_v_sku_cost_breakdown.sql')
on conflict (version) do nothing;
