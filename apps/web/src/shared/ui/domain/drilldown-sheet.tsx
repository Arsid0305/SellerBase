'use client';

/**
 * DrilldownSheet — боковая панель карточки товара с табами
 * (Сводка / По дням / По неделям / Запросы).
 * TODO: подключить @radix-ui/react-dialog (Sheet) + Tabs + react-query данные.
 */
export function DrilldownSheet({ open: _open }: { open?: boolean }) {
  return null;
}
