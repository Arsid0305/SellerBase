/**
 * Placeholder for Supabase generated types.
 * Will be replaced by `supabase gen types typescript --project-id <id>` output
 * once a CI step is wired up. Keeps the import path stable from day one.
 */
export type Database = Record<string, never>;
