// Остатки в рублях: в продаже, в пути, заморожено, потеряно.
//
// Строки приходят из базы готовыми и в нужном порядке - представление
// v_stock_money. Порядок и названия строк заданы владелицей 17.09.2026,
// менять их на стороне страницы нельзя: она читает эту таблицу как есть.

export type StockMoneyRow = {
  /** Порядок строки, как задан в базе. */
  order: number;
  /** 'itog' - жирная строка группы, 'stroka' - вложенная. */
  kind: 'itog' | 'stroka';
  label: string;
  units: number;
  rub: number;
  /** Оговорка под строкой: «не двигаются 32 дн.», «уточняется». */
  note: string | null;
};
